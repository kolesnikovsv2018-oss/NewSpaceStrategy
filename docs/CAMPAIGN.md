# Стратегическая модель, карта и экономика — S3.1–S3.25

**Последний шаг на 2026-09-10: S3.31 завершён — чистый control/run API.** [CampaignControl](../src/domain/campaignControl.ts) и [CampaignRun](../src/domain/campaignRun.ts) реализуют строгие схемы/типы, `getControllerKind`, создание, авторизованные ручные команды/AI-пакет и переход только в local. **Игра остаётся S3.29 с ручным AI/save1/1:** новые модули не импортированы игровым entry, не добавляют UI/IO/save/scheduler. Раздел3 [контракта controller](CAMPAIGN_CONTROL.md#3-будущий-чистый-слой-управления) реализован; разделы4–7 о наблюдении/сцене/паузе/save2 ещё будущие. Основной агент подтвердил2003 теста/36 файлов/282 сценовых:210 новых =38 control +143 run +29 fault. Strict/build прошли;71 модуль, `main-9990d074`,1678.14 kB/gzip397.33 kB, прежний warning. Браузер не запускался — pure API. Следующий **S3.32 — только новые чистые encodeCampaignRunSave/decodeCampaignRunSave для2/1, строгая миграция1/1→local и policy/UTF-8/whole-run тесты**, без репозитория/UI/scheduling/нового слота; legacy API остаётся1/1. [План](../reports/2026-09-07/plan.md).

В [новых тестах S3.31](../tests/campaignRun.test.ts) все command kinds проверены для обеих сторон × обоих режимов: полная run-схема → strict command/request → stale → active → controller → прежний исполнитель. В local помощник доступен обеим активным сторонам; в human-vs-ai ручной red получает `FACTION_CONTROLLED_BY_AI`, AI-blue — `AI_NOT_ASSIGNED`. `convertRunToLocal` сохраняет всю session по значениям без команды/дохода/turn/fuel, даже на cap/terminal; автоматического продолжения после blue-end нет. Request явно задан через `factionIdSchema` и целый1..MAX_TURN, не зависит от позиции варианта command-union.

Оплаченные preset/library циклы: обе стороны реальными командами в **local** зарабатывают380/190 на29, покупают по2 fighter185/11, получают FIFO31; явный local AI продолжает до45/170/248, затем ручные deploy/group/send и явные AI-end дают47/188/258 обеим,4ships fuel2, неизменные snapshots. Назначение red AI — только диагностическая смена control после оплаченной локальной подготовки/отправки, не публичная local→AI операция и не покупка политикой. Библиотека после оплаты изменена/удалена только во временном порте. Checkpoints через `campaignRunSchema.parse` сравнивают runtime-снимки, **не encode/decode, слот или reload**. Отдельно проверены full hidden-state validation, frozen/deep copies, реальные late cap/дефицит25/100/75/FIFO/fuel/last turn/takeover-refuel либо end без colonize, noIO и статический runtime-граф;29 fault-тестов проверяют исключения отдельно. [Матрица выполненного и будущего](CAMPAIGN_CONTROL.md#10-фактические-проверки-s331).

**Текущий статус на 2026-09-10: S3.29 завершён — ручной UI одного AI-хода.** `campaign-ai` x1050/y526 доступна только для **наблюдаемой активной** стороны; inactive/pending disabled. Inline confirm/cancel не запускает план до подтверждения, pending блокирует фон. Захваченные request/source/generation/operation identity/disposed проверяются до и после единственного `executeAiTurn`; успешный state принимается одной записью, не промежуточными `scene.execute`. Наблюдение и выбранная система сохраняются, следующая сторона автоматически не запускается. Домен/схемы/правила/save1/1 не менялись; нет AI IO.

Успех закрывает production/budget/travel/fleets/fleetTravel, сбрасывает страницы/draft/catalog и старую квитанцию. Own `campaign-ai-summary` x892/y340 **вместо выбранных сведений** показывает≤3 выполненных команд с ID целей и фактическую экономику завершённого хода, не следующий forecast. Сводка очищается при выборе системы/смене стороны/переключении производства, ручной команде даже при отказе, успешном load/reset/shutdown; переключение бюджета сохраняет её. Cancel/ошибка AI сохраняют state/выбор/допустимые drafts/панели. [Точный контракт](CAMPAIGN_AI.md#8-saveload-и-будущий-ui).

**1793 теста/33 файла,282 сценовых (+43)**; существующий integration assertion теперь проверяет `MainScene → executor → planner`. Полный test/strict typecheck/build прошёл:71 модуль, `main-9990d074`,1678.14 kB/gzip397.33 kB, прежний warning >500 kB. Обе стороны/четыре стартовых поручения, lifecycle/inactive/stale/same-turn/pending/одно принятие/поздний cap/дефицит; оплаченные preset/library FIFO и группы с save/load сравниваются с непрерывной веткой. Это fake Phaser, не browser hit testing.

**Браузер S3.29:** ограниченный mouse-прогон open/cancel/confirm blue1/red2/blue3, блокировка фона; blue3 colonize eden/explore nexus→130/65, сводка175×126px/низ466 до controls478. Отдельно основной агент прошёл оплаченный пресетный цикл обеих сторон реальными командами сцены без подстановки gameplay state: доход29/по2 заказа/FIFO45, blue deploy[2,1]/group/send→кнопочный save в default localStorage→реальный reload/новая turn1/load-confirm→AI-cancel без изменений/AI-confirm46; red deploy[4,3]/group/send→overwrite-confirm→второй reload/новая turn1/load-confirm→AI-confirm47. Обе188/258,4ships fuel2 в Эдеме/Узле без transit; полные canonical state/summary равны непрерывному executor, старый confirm инертен, наблюдение не переключается. Нет внедрённого порта/авто-AI; sessionStorage только oracle, не источник записи слота/игры. Удалены только тестовый слот/проверочный ключ, оба хранилища пусты. Контрольный снимок до очистки:47/red, summary46 с explore dust/end и оплатой2/2,175×110px/низ450,1panel/1ESC/1canvas. Затем new/reset и меню проверены:0panel/0ESC/1canvas, оба хранилища пусты — [отчёт S3.29](../reports/2026-09-10/04-campaign-ai-ui.md). Не полный mouse E2E/физический ESC/реальная квота/perf; заполненная библиотека в новых циклах проверена тестами, не браузером. [Матрица S3.29](CAMPAIGN_AI.md#12-фактические-проверки-ui-s329).

**История S3.28: завершён только как чистый API одного AI-хода.** [Planner](../src/domain/campaignAiPlanner.ts) без runtime-imports строит один исходный план≤1 colonize →≤1 explore →1 endTurn по минимальной `DeepReadonly` own observation. [Executor](../src/domain/campaignAiExecutor.ts) строго проверяет полный state/request/весь план до dispatch, явно копирует разрешённые поля и рекурсивно замораживает observation/context/разобранные команды. Выполнение только через session-команды на detached candidate; success — после окончательной schema, turn+1, строгой квитанции и проверки ожидаемого реального платежа прежними правилами. Весь пакет атомарен, включая поздний cap после colonize; ошибки фиксированные, state/summary/квитанция независимы. [Контракт и доказательства](CAMPAIGN_AI.md).

**1750 тестов/33 файла =1552 прежних+198 новых**: planner22/executor85/fault70/integration21;239 сценовых неизменны. Основной агент повторно выполнил полный test/strict typecheck/build, блокирующих замечаний проверки нет. Реальный старт четырьмя AI requests даёт начало5 с130/65 каждой стороне. Preset/library заработанные циклы обеих сторон: FIFO31→45, ручные deploy/group/send и AI endTurn→47/188/258/fuel2; codec/новый менеджер восстанавливают31/45/46 при смене часов без расхождения с непрерывной веткой. Отдельно реальные cap обоих ресурсов до/после colonize, MAX_TURN/точные пределы/диагностический дефицит25/100/75/FIFO/free+group arrivals, парные скрытые state и70 fault-тестов. Сборка69 модулей, `main-2236be96`,1670.58 kB/gzip394.87 kB, прежний warning: AI не импортируется игровым entry. **Браузер намеренно не запускался, UI остаётся S3.26 без AI-кнопки.**

**S3.26: ручное сохранение кампании принято как один локальный слот.** Верхние кнопки «Сохранить кампанию»/«Загрузить кампанию» (`campaign-save` x475/y24, `campaign-load` x675/y24) подключены к готовым менеджеру/кодеку. Подтверждение inline, без второго modal/ESC-handler. При запросе save фиксируется `structuredClone` обеих сторон до чтения занятости через `manager.load`: отсутствующий слот записывается сразу, занятый valid/corrupt/future/oversized требует подтверждения, STORAGE_READ_FAILED прерывает запрос. Запись только через `manager.save`; load декодирует кандидат **до** подтверждения и применяет именно его без reread. Отмена/ошибки не сбрасывают партию/черновики; pending блокирует фон/reset. Generation, identity операции/source и disposed защищают same-turn callbacks/reset/shutdown. При успехе load очищаются панели/страницы/drafts/routes/catalog/квитанция; выбирается активная сторона и первая своя система в статическом порядке, иначе home ID без выдачи ownership/exploration. Конструктор/вход/reset без IO; вход **всегда новая партия**, продолжение вручную. API/схемы/правила/библиотека сохранения неизменны.

Проверки S3.26: **1552 теста/29 файлов,239 сценовых (+47)**, strict/build прошли;69 модулей, основной chunk `main-2236be96`, JS1670.58 kB/gzip394.87 kB, прежний warning. Два preset/library UI-цикла обеих сторон с реальными командами и временным портом: оплаченный FIFO checkpoint31 → shutdown/reentry/load →45 → группы/save/load перед каждым own endTurn →47/188/258 каждой стороне/fuel2. Это fake Phaser, не browser hit testing.

**Настоящая persistence проверена отдельно:** изначально пустой реальный localStorage, без внедрённого порта; заработанный пресетный цикл командами сцены до FIFO45 → blue deploy[2,1]/group/send → кнопочный save4058байт → настоящий reload/свежий вход1/load-cancel/load-confirm → endTurn46. Затем red deploy[4,3]/group/send → overwrite-cancel/confirm → второй настоящий reload/свежий вход/load-confirm active red → endTurn47. Итог188/258 каждой стороне, ships в Эдеме/Узле с fuel2, без transit; canonical JSON загруженного/следующего state равен непрерывной ветке. Gameplay state не подставлялся, сохранённый документ не записывался повторно после reload; sessionStorage содержал лишь проверочные expected snapshots/oracle. Созданный слот и проверочный ключ удалены, оба хранилища снова пусты; reset1/1panel/1ESC/1canvas. Библиотека в браузере пустая/неизменная; заполненная/повреждённая проверена тестами.

Отдельный временный порт подтвердил mouse-кнопки/confirm/cancel/блокировку, инъецированные read/quota и кандидат без reread. Его ручной перенос между двумя reload **не persistence**. Основной реальный цикл использовал события кнопок после неудачных mouse-попыток; это не полный mouse E2E/физический Escape/реальная квота/профилирование. Phaser ESC проверен тестами. [Полная матрица S3.26](CAMPAIGN_SAVE.md#14-фактические-проверки-s326).

**S3.27 — исторический проектный контракт, реализованный API S3.28 и UI S3.29.** Новое знание не переоценивается внутри пакета; производство/управление флотами вне политики, обычный endTurn продолжает уже оплаченные FIFO/маршруты. Нет controller state в сцене/save1/1, часов/каталога в политике и автозапуска. S3.30 выбрал [контракт controller](CAMPAIGN_CONTROL.md), S3.31 реализовал его чистый control/run API, но не UI/scheduler/save2. Далее только S3.32 — чистый run-кодек2/1/миграция строгого1/1→local и регрессии без репозитория/сцены/нового слота. **Ручной помощник не постоянный противник; AI-родитель, победа и весь S3 открыты.** Старый заголовок сохранён ради якоря; следующие записи S3.24/S3.25 исторические, не текущие ограничения UI.

**S3.25: однослотовый [CampaignSaveManager](../src/utils/CampaignSaveManager.ts) реализован поверх [кодека S3.24](../src/domain/campaignSave.ts), без интеграции с игровым UI.** Необязательный `StoragePort` внедряется явно либо лениво берётся global localStorage; конструктор не обращается к нему. `load()` — один `getItem` ключа `orion_campaign_v1`, null → `SAVE_NOT_FOUND`, исключение getter/чтения → `STORAGE_READ_FAILED`, иначе настоящий decode. `save(unknown)` сначала encode, даже до global getter, затем один `setItem`; успех только `{ok:true}`, исключение → `STORAGE_WRITE_FAILED` без сырых деталей. Нет чтения перед записью, удаления/rollback/fallback/кеша; атомарность обязательна для порта, last successful writer wins. Подтверждение перезаписи остаётся будущему caller/UI. [Контракт](CAMPAIGN_SAVE.md), [отчёт S3.25](../reports/2026-09-09/23-campaign-save-repository.md).

Полный прогон S3.25: **1505 тестов/29 файлов =1435 прежних+70 новых**,192 сценовых неизменны; strict typecheck/build прошли,67 модулей, JS1665.95 kB/gzip393.48 kB, прежний warning >500 kB. Основной chunk `main-2586e329` прежний: менеджер не импортирован игровым entry. Два заработанных preset/saved-library цикла обеих сторон проходят checkpoint слота на turn31 → FIFO45 → deploy/group/send → checkpoint перед каждым своим endTurn →47:188/258 каждой стороне и fuel2, полные результаты/квитанции равны непрерывной ветке. Реальные codec/UTF-8 exact5MB/+1, ошибки storage, независимость, счётчики/transit/дефицит/cap/terminal и два менеджера проверены отдельно; [матрица S3.25](CAMPAIGN_SAVE.md#12-фактические-проверки-s325).

**Новый браузерный smoke S3.25:** менеджер/сессия импортированы в существующую страницу с явно переданным временным Map-портом. Конструктор0 операций, missing load; новая локальная сессия реальными explore/colonize/endTurn обеих сторон до3 → save/new manager/load/равенство следующего endTurn. Инъекция `DOMException` с `QuotaExceededError` оставляет старый слот при одной попытке set; invalid save не добавляет записей. Итого2 чтения/2 попытки записи (успех+отказ), снимок пользовательского localStorage неизменен, сцена не тронута. Не реальная квота/диск/reload/UI/E2E, не групповой браузерный цикл и не профилирование.

Исторический S3.24 проверял **только чистые модули** в свежей странице: turn3/round-trip/следующий endTurn, exact5MB/+1 и приоритет rules2;1435 тестов/28 файлов. [Его отдельные результаты](CAMPAIGN_SAVE.md#11-фактические-проверки-s324) не являются smoke репозитория. На этапе S3.25 UI S3.26 и пользовательский save→reload→load ещё оставались открыты; теперь приняты выше, без закрытия AI/победы и всего S3.

**Реализованная игра после S3.22:** экономика и бюджет приняты в мирном групповом цикле обоих источников (пресет/библиотека) для обеих сторон. Экономический родитель закрыт только в границах двух ресурсов, предоплаченного FIFO, отмены/возврата, размещения, покорабельного содержания и бюджетного UI; [матрица доказательств](CAMPAIGN_UPKEEP.md#приёмка-s322-и-матрица-доказательств). Кнопка «Бюджет» отображает own `economyForecast`; условный inactive-прогноз не разрешает командовать, фактическая `endTurnEconomy` отделена от нового прогноза. [Контракт содержания](CAMPAIGN_UPKEEP.md) не менялся:1 кредит/own ship/own успешный `endTurn`, gross cap до списания, частичная оплата без долга/потерь/остановки FIFO и прибытия. **S3 целиком открыт.** История S3.22 ниже описывает выбранный тогда следующий проектный шаг; его результат теперь S3.23 выше.

Групповая отправка и атомарное прибытие доступны через Производство → Группы → Маршруты. Создание/просмотр/расформирование — в Группах; заправка каждого участника — в Перелётах. Баланс топлива не менялся. **Разделы S3.1–S3.21 ниже фиксируют последовательные этапы:** старые ограничения «нет топлива/групп/UI/содержания», отсутствие повторной групповой браузерной приёмки и числа тестов относятся к своему шагу, не к текущему статусу. Доменные правила S3.20 и UI S3.21 действуют сейчас; нынешняя приёмка описана в S3.22 в конце документа. Исторические отчёты не переписываются.

Первый шаг S3 — чистая фиксированная галактика с двумя сторонами и проверяемыми командами. [Реализация](../src/domain/campaign.ts) не импортирует Phaser, корабли, хранилище, часы или RNG. S3.2 подключает её к карте [MainScene](../src/scenes/MainScene.ts) через [CampaignPanel](../src/ui/CampaignPanel.ts). Это локальная песочница, ещё не законченная кампания.

S3.3 добавляет чистый [слой пошаговой партии](../src/domain/campaignSession.ts) над той же галактикой: ресурсы, доход колоний и завершение хода. **С S3.4 карта использует CampaignSession и проверяет очередность всех игровых команд.** Наблюдаемая сторона выбирается локально, но это не меняет активную сторону. S3.5–S3.8 добавляют производство и размещение; S3.9–S3.13 — одиночные перелёты, топливо и UI заправки. S3.14–S3.17 — группы, модель и интерфейс группового перелёта; S3.20 — содержание кораблей. Снабжение, полноценный AI-противник и победа ещё не реализованы; сохранение кампании использует кодек S3.24, репозиторий S3.25 и ручной UI S3.26, AI API S3.28 подключён к карте ручной кнопкой S3.29.

## Сценарий

Карта содержит шесть систем и двусторонние переходы. Координаты — условная раскладка стратегической карты, не расстояние полёта или тактические координаты.

| Система | Координаты | Пригодность | Начальный владелец |
| --- | --- | --- | --- |
| Сол (sol) | 0, 0 | Да | blue |
| Эдем (eden) | 1, 1 | Да | Нет |
| Разлом (rift) | 1, −1 | Нет | Нет |
| Узел (nexus) | 2, 0 | Да | Нет |
| Пыль (dust) | 2, −2 | Нет | Нет |
| Вега (vega) | 3, 0 | Да | red |

Переходы: Сол—Эдем, Сол—Разлом, Эдем—Узел, Разлом—Пыль, Пыль—Узел, Узел—Вега. Это небольшой проверочный сценарий, не сбалансированная карта и не генератор галактик. Blue начинает с разведанным Солом, red — с разведанной Вегой; остальные системы изначально неизвестны обеим сторонам.

## Контракты и владение данными

- `getGalaxyDefinition()` возвращает независимую полную карту: стороны, дома, системы, переходы. Она предназначена для доверенной модели/инструментов, не для отображения скрытой информации игроку. Мутация результата не меняет внутренний сценарий.
- `createCampaignState()` каждый раз создаёт новое независимое состояние. Запись системы содержит `id`, `ownerId` (blue/red/null) и `exploredBy`. Статические имена/координаты/пригодность не дублируются в состоянии.
- `campaignStateSchema` строго проверяет ровно шесть уникальных известных систем, допустимые стороны, уникальность разведавших сторон и инвариант колонии: система пригодна и разведана владельцем. Порядок записей не задаёт их идентичность; поиск идёт по ID.
- `CampaignCommand` — строгий discriminated union с `kind: explore | colonize`, `factionId`, `systemId`. Лишние поля, неизвестные идентификаторы, пропущенные значения и неверные типы не преобразуются автоматически.
- `executeCampaignCommand(state, command)` принимает неизвестные входы и сначала проверяет состояние, затем команду. Возвращает `{ ok: true, state }` либо `{ ok: false, code, message }`. При любом исходе входы неизменны. При успехе всё состояние независимо скопировано, а содержательно меняется только целевая система; вызывающий код принимает новый `state` только при `ok`.
- `getCampaignView(state, factionId)` выдаёт независимую проекцию стороны; неверные аргументы запроса вызывают ZodError. Это отличается от команд, возвращающих типизированный отказ.

Схема состояния — runtime-инварианты фиксированного сценария, **не версия формата сохранения кампании**. Нет ID партии, миграций, localStorage, восстановления боя, фракционного AI или авторизации. В локальной модели вызывающий код явно передаёт действующую сторону; это не сетевой протокол безопасности.

## Предусловия команд

В S3.1 команды мгновенные, без флота, стоимости, дальности, топлива, очереди или хода. Эти упрощения явные: добавление экономики/перелётов позже потребует новых контрактов, а не объявления нынешних команд готовыми миссиями кораблей.

| Команда | Предусловия | Единственное содержательное изменение |
| --- | --- | --- |
| explore | Цель ещё не разведана действующей стороной; имеет прямой переход хотя бы к одной разведанной ею системе | Добавить действующую сторону в `exploredBy` цели |
| colonize | Цель разведана действующей стороной, свободна, пригодна и имеет прямой переход к её собственной колонии | Установить `ownerId` цели |

Разведку можно продолжать через нейтральные, вражеские и непригодные системы: владение для неё не требуется. Колонизация требует соседнюю **собственную колонию**, одного разведанного пути недостаточно. Чужую или уже свою колонию повторно колонизировать нельзя; захвата нет. Стороны разведывают одну систему независимо.

Коды отказов: `INVALID_STATE`, `INVALID_COMMAND`, `ALREADY_EXPLORED`, `OUT_OF_REACH`, `NOT_EXPLORED`, `OCCUPIED`, `UNINHABITABLE`. Для колонизации видимость проверяется раньше владельца/пригодности: скрытая система не раскрывает эти сведения через причину отказа. Отказ ничего не меняет и не возвращает частично изменённое состояние.

## Видимость

Топология, имена и координаты звёзд публичны. `CampaignSystemView` — union:

- `unknown`: только id/name/x/y/visibility, **нет полей** ownerId/habitable;
- `explored`: дополнительно текущие ownerId и habitable.

Нельзя отдавать UI полный `CampaignState` или `GalaxyDefinition` вместо этой проекции для отображения скрытых данных. Разведка постоянна, сенсорной дальности и исчезновения видимости пока нет. Уже разведанная система показывает актуального владельца, даже если он изменился позже: это не сохранённый снимок «последний раз видели». Проекция не раскрывает `exploredBy` других сторон. Полная модель всё равно доступна доверенному локальному коду — это разграничение представлений, не защита от клиента в multiplayer.

## Проверки карты на этапе S3.2

[campaign.test.ts](../tests/campaign.test.ts): 45 доменных регрессий; [campaignScene.test.ts](../tests/campaignScene.test.ts): 11 контрактных тестов реальных scene/panel callback с подменой Phaser. Всего 441 тест в 18 файлах, typecheck/build пройдены. Настоящий браузер S3.2: mouse-клик выбирает Эдем, программные события/конечные шаги скрытой вкладки проверяют обе стороны, отказы, скрытые свойства, сброс/отмену/выход и повторный вход. Не полный mouse E2E, не FPS-тест. [Отчёт S3.2](../reports/2026-09-08/13-campaign-map.md).

### Карта: общие правила интерфейса

В меню кнопка «Галактика» запускает новую локальную партию. Выбор — кнопкой с названием системы; круг отмечает выбранную. Цвет/маркер обозначают только видимое владение/разведку. Справа — сведения выбранной системы, «Разведать»/«Колонизировать» и ответ модели. Команды намеренно доступны даже при недопустимой цели: модель возвращает причину отказа, UI не дублирует предусловия.

MainScene владеет CampaignSession: ручные команды вызывает через executeSessionCommand, подтверждённый AI-пакет S3.29 — через executeAiTurn; принимает новое состояние только при ok. CampaignPanel получает лишь CampaignSessionView, выбранный ID/сообщение, own summary и callbacks. При переключении стороны выбор сохраняется, но проекция и сообщение заменяются, summary очищается: прежние сведения не остаются в тексте, цвете или маркере. Это локальный режим наблюдения/управления обеими сторонами с разовым AI-помощником, не постоянный противник; право командовать определяется очередностью модели. До S3.4 карта использовала только базовый sandbox API.

«Новая партия» и «Меню» требуют подтверждения потери текущей партии. Пока оно открыто, прочие кнопки неинтерактивны. ESC отменяет подтверждение; без него закрывает вкладку производства, а на карте запрашивает выход, не выходит сразу. Сброс возвращает blue/Сол и начальную карту. Выход уничтожает панель, снимает ESC и освобождает модель; повторный вход новый. Панель пересоздаётся только по действиям, не каждый кадр; старые callbacks после destroy бездействуют. UI рассчитан на существующий логический canvas 1280×720 с Phaser FIT.

С S3.26 партия сохраняется **только вручную** в отдельный локальный слот. Закрытие/перезагрузка теряет несохранённые изменения без beforeunload; при входе новая партия, для продолжения нужно явно загрузить слот и подтвердить замену. Сброс/выход не удаляют и не перезаписывают сохранение, корабельная библиотека не меняется. Производство и интерфейс очереди описаны ниже. [План](../reports/2026-09-07/plan.md), [история S3.1](../reports/2026-09-08/12-campaign-domain.md).

## Экономика и пошаговая партия S3.3

`CampaignSession` содержит единственную `galaxy: CampaignState`, номер `turn` и `treasuries` двух сторон. Статические системы и правила разведки/колонизации не дублируются. `createCampaignSession()` создаёт независимые партии и вложенные казны. Это пока не версионированный формат сохранения.

### Начальные правила

| Величина | Правило |
| --- | --- |
| Ресурсы | Целые кредиты и минералы, каждый в пределах 0..1 000 000 000; без преобразования строк и без отрицательных остатков |
| Стартовая казна каждой стороны | 100 кредитов и 50 минералов; начальный доход дополнительно не начисляется |
| Доход одной колонии | 10 кредитов и 5 минералов при завершении хода её владельца |
| Ход | Одно окно действий стороны: 1 — blue, 2 — red, 3 — blue и т.д. Не полный раунд обеих сторон |
| Предел номера хода | 1..1 000 000 000; активная сторона вычисляется по чётности, не хранится вторым изменяемым полем |
| Разведка/колонизация | Прежние географические правила, бесплатные и без лимита действий, но только активной стороной |

Числа — предварительные параметры, не результат балансировки. Кредиты/минералы не связаны с тактической энергией, cargo-партиями и неисполняемыми служебными модулями. В S3.3 расходов не было; S3.5 добавляет оплату заказов и возврат за отмену, S3.20 — содержание после проверки валового дохода на cap. Рост населения и ресурсные залежи ещё не реализованы.

### Команды и атомарность

В S3.3 `SessionCommand` — строгий union explore/colonize/endTurn; S3.5 добавляет enqueueProduction/cancelProduction с payload, описанным ниже. Каждая команда содержит `factionId` и обязательный `expectedTurn`; у двух географических команд также `systemId`. `endTurn` не принимает systemId или произвольные поля. `executeSessionCommand` проверяет в порядке: схема партии → схема команды → совпадение номера хода → активная сторона → правила действия.

- Разведка/колонизация делегируются `executeCampaignCommand`; из запроса удаляется только поле слоя сессии expectedTurn. Доменные причины отказа сохраняются. Номер хода и казны не меняются; результат галактики принимается лишь при успехе.
- `endTurn` берёт колонии, принадлежащие завершающей ход стороне **в текущем состоянии**, прибавляет доход только её казне и увеличивает номер хода на один. С S3.20 после валовой проверки также списывает содержание всех её ships. Новая колония приносит доход уже при первом завершении своего хода; разведанная нейтральная/чужая система не приносит дохода. Ноль колоний означает нулевой доход, не поражение.
- Номер хода проверяется первым, затем оба валовых ресурса до записи/списания. Если хотя бы один ресурс превысит предел или номер хода нельзя увеличить, возвращается отказ целиком — без части дохода, clamp или смены стороны. Содержание не спасает переполнение. Ресурсный лимит может остановить дальнейшие ходы; это явная техническая граница прототипа, не условие победы.
- Успех возвращает полностью независимую копию `{ ok: true, state }`, с S3.20 только успех endTurn дополнительно содержит `endTurnEconomy` с завершённым номером хода и фактическим платежом. Отказ — `{ ok: false, code, message }` без state/квитанции. Входные объекты никогда не меняются.

Помимо кодов карты добавлены `STALE_TURN`, `NOT_ACTIVE_FACTION`, `RESOURCE_LIMIT`, `TURN_LIMIT`. Повтор старого endTurn на **обновлённом** состоянии не начисляет доход снова, даже когда та же сторона вновь активна. Вызывающий код обязан принять возвращённое состояние: повтор функции на старом снимке закономерно воспроизводит прежний результат. expectedTurn — не ID партии/запроса и не сетевой протокол дедупликации; команды той же стороны внутри текущего хода не получают отдельной revision. Клиентский state не является доверенной multiplayer-авторизацией.

### Проекция и интеграция

`getCampaignSessionView(state, factionId)` возвращает проекцию галактики, публичные номер хода/активную сторону и **только собственные** казну/расчётный валовой доход запрошенной стороны. С S3.20 обязателен также `economyForecast`: успешный расчёт income/upkeep/treasuryAfter либо TURN_LIMIT/RESOURCE_LIMIT; неактивная сторона получает гипотетическую оценку, не право на команду. Запрос не начисляет доход и не меняет состояние. Противоположные казна/доход/содержание/дефицит не раскрываются даже после разведки её колоний. Возвращаемые объекты независимы; неверные state/faction бросают ZodError, как запрос базовой модели.

UI хранит CampaignSession; ручные команды идут через executeSessionCommand, AI-пакет S3.29 — через executeAiTurn, который использует тот же session API без промежуточной публикации. Прямой вызов executeCampaignCommand сценой обошёл бы проверку очередности. Базовый API остаётся внутренним географическим слоем сессии и самостоятельным контрактом тестов карты, но больше не используется сценой напрямую.

### Проверки на этапе S3.3

[campaignSession.test.ts](../tests/campaignSession.test.ts) — 60 новых чистых тестов порядка хода/дохода/новых колоний, чужих команд, replay, переполнения, схем, frozen-входов, независимости казн/состояний/проекций и сокрытия чужой экономики. Всего 501 тест в 19 файлах, typecheck/build проходят. Браузер в этом модельном шаге не проверялся; UI не менялся. Неизменный production JS ожидаем: слой сессии пока не импортируется сценой. [Отчёт S3.3](../reports/2026-09-09/01-campaign-economy.md).

## Пошаговая экономика в интерфейсе S3.4

- Над картой показаны номер хода и активная сторона; справа — наблюдаемая сторона, только её кредиты/минералы и ожидаемый доход при завершении **её** хода. После колонизации доход обновляется сразу, баланс — лишь после успешного endTurn.
- «Завершить ход» передаёт endTurn через общий исполнитель. После успеха наблюдаемая сторона и выбор системы остаются прежними: можно увидеть начисление. Для управления следующей стороной нажмите «Сменить сторону · локально». Эта кнопка сама не меняет номер хода и не начисляет доход.
- Действия неактивной стороны намеренно не скрыты: модель отклоняет их сообщением «Сейчас ход другой стороны». Географические отказы, устаревший номер хода и переполнение также показаны без замены состояния при ошибке. UI не дублирует игровые проверки.
- При создании панели callback захватывает показанные expectedTurn/factionId/systemId, а не читает изменяемые поля сцены в момент позднего вызова. После перерисовки старый callback блокируется disposed-защитой панели; после reset/выхода он не исполняется даже при совпадении номера хода новой партии. expectedTurn дополнительно защищает от изменения номера хода вне текущей панели.
- Подтверждение новой партии/выхода блокирует также завершение хода. Reset восстанавливает turn1/blue/Сол, обе казны100/50 и доход10/5; выход снимает ESC и освобождает сессию. Перерисовка происходит по действиям, не по кадрам.

### Проверки S3.4

[campaignScene.test.ts](../tests/campaignScene.test.ts) теперь содержит 21 тест (10 новых, прежние адаптированы к сессии). Всего 511 тестов/19 файлов, strict typecheck и production build проходят. Размер JS 1627.13 kB/gzip382.77 kB; предупреждение chunk >500 kB осталось.

Настоящий Phaser: мышью проверены вход из меню, выбор Эдема и endTurn. Программными событиями проверены разведка/колонизация обеих сторон и доход120/60 каждой после основания второй колонии, чужой endTurn без изменений, смена проекции без утечки, повтор callback без двойного дохода, блокировка/отмена/сброс/выход/повторный вход. Устаревший ход, предел ресурсов и MAX_TURN проверены диагностической временной подстановкой состояния с восстановлением снимка; это не обычный пользовательский путь. Снимок localStorage не изменился в диагностическом сценарии. Вкладка активна, ручные game.step в завершающем прогоне не использовались. Тексты проверенных состояний внутри1280×720; screenshot осмотрен; после повторного входа одна панель, один ESC-handler, один canvas. Не полный mouse E2E, не FPS-тест. [Отчёт S3.4](../reports/2026-09-09/02-campaign-economy-ui.md).

## Очередь производства S3.5

[production.ts](../src/domain/production.ts) — чистые схемы, цена, возврат и FIFO-шаг. [campaignEconomy.ts](../src/domain/campaignEconomy.ts) хранит общий контракт ресурсов без циклической зависимости; прежние экспорты treasurySchema/MAX_RESOURCE/Treasury из campaignSession сохранены.

### Правила заказа

| Параметр | Принятое правило |
| --- | --- |
| Проект | Снимок schemaVersion2, `validateDesign(..., 'flight')`; гражданские без оружия допустимы. Боевой валидатор не ослабляется |
| Кредиты | `ceil(calculateShipStats(design).cost / 100)` |
| Минералы | `ceil(calculateShipStats(design).mass / 10)` |
| Срок | `max(1, ceil(credits / 50))` собственных завершений хода |
| Оплата | Полностью при постановке, до будущего дохода. Нехватка любого ресурса отклоняет весь заказ |
| Очередь | До3 заказов на колонию. Один первый заказ продвигается на один шаг за свой endTurn; остаток мощности следующему не передаётся |
| Готовность | После последнего шага заказ удаляется из очереди и один раз добавляется в completed с тем же ID/проектом/владельцем/колонией; это не runtime-корабль |
| Отмена | Только незавершённый свой заказ в указанной своей колонии; возврат каждого ресурса `floor(cost * remainingTurns / totalTurns)` |
| Вместимость | До100 pending+completed на сторону, резервируется при заказе. Готовая запись уже имеет место, поэтому завершение не блокируется переполнением списка |

Эти коэффициенты явно переводят цену/массу корабельного расчёта в стратегические единицы; не меняют формулы корабля и не моделируют реальные залежи/снабжение. Старт остаётся100/50, доход10/5; дорогие проекты требуют накопления. Баланс предварительный, боевая полезность и длительность партии не измерялись.

### Данные и команды

- `CampaignSession.production = { lastOrderId, orders, completed }` обязательно. Каждый заказ — `{ id, factionId, systemId, design, remainingTurns }`; готовая запись — те же поля без remainingTurns. Производные цена/масса/срок не сохраняются, вычисляются из независимого design. Правки каталога/верфи не меняют оплаченный заказ. Изменение формул в будущем потребует решения о версиях правил; нынешняя runtime-схема не формат save.
- ID — монотонное целое1..1e9 внутри партии без часов/RNG. После отмены не переиспользуется; исчерпание блокирует новые заказы, не отмену. Из массива orders проверяются возрастание ID, уникальность ID во всех списках, счётчик не меньше выданных, лимиты, допустимый flight-проект, remainingTurns1..срок. У ожидающих за первым заказов прогресс запрещён. Сессия дополнительно проверяет принадлежность колонии каждой записи; захват колоний пока не реализован.
- `enqueueProduction` содержит factionId/expectedTurn/systemId/design; `cancelProduction` — factionId/expectedTurn/systemId/orderId. Строгие схемы отвергают произвольные cost/refund/progress. Общие проверки expectedTurn/активной стороны идут до производства. Отказ чужой/неизвестной/нейтральной цели одинаковый `NOT_OWN_COLONY`; детали владельца не раскрываются.
- Ошибки дополнительно: `INVALID_DESIGN` (структурно корректен, но не flight), `INSUFFICIENT_RESOURCES`, `QUEUE_FULL`, `PRODUCTION_LIMIT`, `ORDER_NOT_FOUND`, `ORDER_ID_LIMIT`. Неверная структура — INVALID_COMMAND/INVALID_STATE. Quote/refund-запросы бросают ZodError на неверный вход.
- `endTurn` проверяет предел номера и оба итога дохода, затем на независимой копии начисляет доход, продвигает первые заказы своих колоний и увеличивает turn. Переполнение дохода отменяет также прогресс/готовность; переполнение возврата отменяет отмену целиком. MAX_TURN блокирует endTurn, но не все остальные действия.
- Повтор отмены уже удалённого ID отклоняется. Старый endTurn не завершает заказ снова. Два валидных enqueue одного проекта в одном ходу — **два отдельных оплаченных заказа**, не повтор одного request-id. expectedTurn не является сетевой дедупликацией; панель S3.6 блокирует повтор старых callbacks после перерисовки, как карта.
- `getCampaignSessionView` дополнительно выдаёт production с собственными orders/completed без глобального lastOrderId; чужие проекты/очереди не передаются, даже после разведки колонии. Собственные ID глобальной последовательности могут иметь пропуски; это не обещание защиты от косвенного анализа активности противника в multiplayer. Все вложенные снимки отделены.

### Интеграция и границы

Сессия продвигает заказы на endTurn. Общий command callback CampaignPanel намеренно допускает только explore/colonize/endTurn; S3.6 подключает производство отдельными callbacks с проектом/ID. Новые команды нельзя собрать из одного kind без payload. Старые сессии без production отвергаются; кампании не сохранялись, миграция/import не добавлены. При разработке после HMR старой модели начать новую партию; состояние предыдущей runtime-схемы не поддерживается.

Ограничение100 производственных записей включает completed: после заполнения новые заказы блокируются. С S3.7 размещение через доменную команду освобождает место completed; S3.8 подключает кнопку в UI. У ships собственный предел100 на сторону. Содержание, бой, AI, исследования и save остаются вне шага.

### Проверки и следующий шаг

[production.test.ts](../tests/production.test.ts) — 58 новых тестов. Всего569/20 файлов, typecheck/build прошли. Проверены цена/округления, FIFO/несколько колоний/обе стороны, snapshot/frozen/replay, возврат/лимиты/схемы/скрытие чужих проектов, atomic endTurn. В браузере выполнен только программный smoke новой партии/обычного endTurn/reset с пустой production: не проверка отсутствующего производственного UI. [Отчёт S3.5](../reports/2026-09-09/03-production-domain.md).

## Интерфейс производства S3.6

1. Выберите свою колонию на карте и нажмите «Производство». Вернуться к выбору систем — «← Карта» или ESC. Правая панель экономики остаётся доступна.
2. Стрелки выбирают один из семи существующих пресетов или сохранённых проектов. Источник/номер, цена в кредитах/минералах и срок в собственных ходах показаны до оплаты. Неполный проект показан с первой причиной недопустимости; «Заказать» отключена.
3. «Заказать» полностью оплачивает проект через session-команду. Отказ при нехватке ресурсов, полной очереди или неактивной стороне ничего не меняет. После endTurn активная сторона меняется, наблюдение — нет; для следующего хода переключитесь локально.
4. До трёх заказов своей выбранной колонии показывают ID/имя, статус первого/ожидающих, выполненные и оставшиеся ходы. Кнопка отмены показывает точный текущий возврат обоих ресурсов и исполняет отмену немедленно, без отдельного подтверждения.
5. Готовые записи доступны по одной на страницу со стрелками. Они ещё не размещены; с S3.8 кнопка «Разместить» переносит выбранную запись в стратегический корабль (см. ниже). Для чужой/неизвестной/нейтральной системы производственных controls нет; чужие orders/completed не передаются панели.

[ProductionCatalog](../src/utils/ProductionCatalog.ts) читает библиотеку только при первом открытии и «Обновить». Использует существующий ShipDesignManager.load, не save/import/repair; повреждение/недоступность явно показаны, пресеты остаются. Обновление сбрасывает выбор на первый проект, не меняет оплаченные снимки. Каталог обеих сторон общий локальный, не исследованные технологии или фракционные сохранения. Совпадающие названия не служат идентификатором выбора: используется позиция в снимке.

[ProductionPanel](../src/ui/ProductionPanel.ts) получает SessionView, каталог и callbacks, не CampaignSession. MainScene захватывает expectedTurn/factionId/systemId/независимый design, а отмена передаёт orderId. После redraw/reset/выхода disposed блокирует старый callback. Новое нажатие на новую кнопку в том же ходу всё ещё означает новый заказ. Pending блокирует также выбор/refresh/заказ/отмену/пагинацию. Reset/shutdown очищают каталог и дочернюю панель; нет чтения storage по кадрам.

Имена нормализуются и сокращаются только для показа; измерение text.width сохраняет видимую часть имени и многоточие после длинного ID. Исходный проект не изменяется. Указанный срок — длительность самого заказа, без ожидания предыдущих в очереди; очередь явно последовательная.

### Проверки S3.6 и следующий шаг

18 новых тестов: 5 в [ProductionCatalog.test.ts](../tests/ProductionCatalog.test.ts), 13 добавлены в [campaignScene.test.ts](../tests/campaignScene.test.ts), где теперь34. Всего587/21 файл, typecheck/build. Сценовые тесты используют fake Phaser, измерение шрифта в них синтетическое. Настоящий браузер отдельно проверен мышью и программными событиями; библиотека/длинные имена/большие ID — временные диагностические данные без записи localStorage. [Подробный отчёт](../reports/2026-09-09/04-production-ui.md).

## Стратегическое размещение S3.7

[campaignShips.ts](../src/domain/campaignShips.ts) определяет `CampaignShip = { id, factionId, systemId, design, transit? }` и строгую схему списка (поле transit добавлено S3.9, описано ниже). `CampaignSession.ships` обязателен, новая партия содержит пустой список. Проект проверяется общим `flightDesignSchema`, без требования оружия; это не тактический экземпляр DesignedShip и не ShipState. Нет HP, энергии, боезапаса, груза, тактического движения, группировки флота или содержания.

### Атомарная команда

`deployProduction` принимает только `factionId`, `expectedTurn`, `systemId`, `orderId`. Порядок проверки: схема state/command → совпадение хода → активная сторона → своя колония → свой completed с этим ID именно в указанной колонии → предел кораблей стороны. Нельзя задать другой проект, destination, цену или параметры runtime.

При успехе одна запись переносится из completed в ships с прежними ID/стороной/системой/проектом, без transit. Входные state/command не меняются, весь результат независимо скопирован. Казны, номер хода, галактика, pending и остальные completed не меняются. Размещение бесплатно, повторно не оплачивает постройку и не начисляет доход. Порядок размещения свободный, не FIFO: ships могут иметь невозрастающие ID. Корабль размещается в своей колонии; S3.9 разрешает последующий перелёт между собственными соседними колониями.

Отказы — прежние INVALID_STATE/INVALID_COMMAND/STALE_TURN/NOT_ACTIVE_FACTION/NOT_OWN_COLONY, плюс `COMPLETED_NOT_FOUND` и `SHIP_LIMIT`. Проверка чужой/неизвестной колонии не раскрывает содержимое её completed. Незавершённый, отменённый, отсутствующий, уже размещённый или чужой ID не размещается. Перенести проект в другую собственную колонию этой командой также нельзя.

### ID, вместимость, приватность

- ID корабля — ID его производственного заказа. Отдельный счётчик/sourceOrderId не нужен; `production.lastOrderId` остаётся общим источником выдачи. Размещение не увеличивает его и работает при MAX_ORDER_ID; MAX_TURN запрещает только endTurn, не размещение активной стороной.
- Строгая сессия проверяет уникальность ships, отсутствие пересечения с orders/completed, ID≤lastOrderId, flight-проект, известную сторону/систему и собственную колонию. Это проверка текущей согласованности, не доказательство истории оплаты: локальный state не multiplayer-авторитет.
- До100 ships на каждую сторону, максимум200 вместе, отдельно от100 pending+completed на сторону. При достижении ship cap следующий перенос отклоняется без удаления completed. Успех освобождает производственное место; последующий enqueue выдаёт следующий ID, не переиспользует ID корабля. Удаления/потерь кораблей пока нет.
- Повтор на обновлённом state не создаёт дубликат: ID больше нет в completed. При смене хода старый запрос получает STALE_TURN. На старом снимке чистая функция повторяет тот же результат; caller обязан принимать новый state. Это не network deduplication.
- `getCampaignSessionView(...).ships` содержит только собственные глубоко отделённые записи, даже если чужая колония разведана. Глобальные ID собственных кораблей могут иметь пропуски — не гарантия защиты от косвенных multiplayer-утечек.

### Совместимость и проверки

Старые runtime-сессии без ships отвергаются; сохранения кампании/миграции не добавлены, после HMR начать новую партию. S3.8 подключает размещение и отображение ships в ProductionPanel. В MainScene отдельное сообщение нового типа команды, generic callback панели не расширен без payload.

[campaignShips.test.ts](../tests/campaignShips.test.ts) —41 новый чистый тест: полный paid→completed→deployed цикл, обе стороны/колонии, бесплатность, frozen/replay, независимость, повтор/stale, границы100/200 и ID/turn, освобождение production-места, пересечения ID, схемы/ошибки и сокрытие чужих кораблей. Всего628/22 файла, typecheck/build проходят. В браузере только программный smoke прежней карты/производства с пустыми ships, без проверки отсутствующего UI размещения. [Отчёт S3.7](../reports/2026-09-09/05-strategic-deployment.md).

## Интерфейс размещения S3.8

В своей колонии откройте «Производство». Внизу «ГОТОВО К РАЗМЕЩЕНИЮ» стрелками выберите запись и нажмите «Разместить». Это бесплатный перенос в ту же колонию, без смены хода. При успехе запись исчезает из готовых; текущая страница ограничивается оставшимся списком. Нет готовых — кнопка неактивна. При неактивной стороне/лимите кораблей кнопка доступна и показывает доменный отказ, ничего не расходуя и не удаляя.

«Корабли: N» переключает нижнюю часть на размещённые корабли выбранной колонии, «Готово: N» — обратно. Оба списка по одной записи (страница/ID/имя); отдельная подсказка показывает общий счётчик своих кораблей из100, включая transit. После размещения просмотр кораблей открывается на новом экземпляре. Кнопок перелёта и боя пока нет — это явно подписано; модель перелёта добавлена S3.9. Чужие ships не передаются UI, даже если их колония разведана.

Контрактные детали:

- `production-deploy` замыкает ID показанного completed. MainScene добавляет захваченные factionId/systemId/expectedTurn и вызывает deployProduction. Поздний callback не выбирает новый элемент по текущему индексу.
- Старые callbacks блокируются disposed после redraw/страницы/refresh/вкладки/стороны/reset/выхода. Новое нажатие на новую кнопку может разместить следующий готовый проект в том же ходу — это намеренное отдельное действие.
- `showShips`, `shipsPage`, `completedPage` принадлежат сцене; render ограничивает обе страницы текущими списками. Смена колонии/стороны сбрасывает страницы и возвращает режим готовых. Переключение списка не читает библиотеку и не меняет сессию.
- Pending блокирует размещение, переключение списка и страницы. ESC отменяет pending, иначе возвращает всю вкладку производства на карту. Reset/shutdown очищают новые поля вместе с прежним каталогом и дочерней панелью.
- Длинное имя готового проекта ограничено570 px до кнопки, корабля745 px; многоточие рассчитано по text.width. Полный snapshot не меняется. Счётчик лимита стоит в начале подсказки, чтобы не потеряться при обрезке.

### Проверки S3.8 и следующий шаг

16 новых регрессий в [campaignScene.test.ts](../tests/campaignScene.test.ts), всего50 сценовых/644 общих в22 файлах; strict typecheck/build прошли. Fake Phaser проверяет payload, отказы/stale/лимит, страницы/приватность, обе стороны, pending/reset/cleanup и обрезку с синтетической шириной. Отдельный настоящий браузер: mouse-размещение/отказ/список, обычный paid→completed цикл программными событиями, предельные100 кораблей/ID/длинные имена диагностической подстановкой; localStorage неизменен. [Отчёт S3.8](../reports/2026-09-09/06-deployment-ui.md).

## Ограниченный перелёт S3.9

Это предварительный транспорт **одного корабля одной командой** между двумя соседними собственными колониями. Нет группировки, движения через несколько переходов, отмены/перенаправления, топлива или содержания; тактическая скорость/батарея/груз не используются. Несколько кораблей можно отправить отдельными командами за один ход.

### Состояние и совместимость

- Без `transit` корабль стоит в `systemId`. При наличии `transit = { destinationId, remainingTurns: 1 }` он в пути: `systemId` — исходная колония, **не физическое присутствие** в ней. Не дублируются status/sourceId. Ноль оставшихся ходов не хранится: при прибытии systemId заменяется целью, transit удаляется.
- Строгая ship-схема проверяет известные ID, literal1, запрет лишних полей и прямой переход в другую систему. `areSystemsAdjacent` использует те же двусторонние lanes, что географическая модель, не отдельную копию карты. Сессия требует свои колонии на обоих концах. Потеря ownership любого конца делает такую сессию недопустимой; захват не реализован.
- ID, проект, сторона, порядок и количество кораблей при отправке/прибытии сохраняются. Transit учитывается в общем пределе100 кораблей стороны; вылет не освобождает место для размещения. ID не переиспользуется, не пересекается с pending/completed.
- Прежние стационарные записи S3.8 без transit остаются валидными, поле не заполняется автоматически. Сессии без обязательных ships/production по-прежнему отвергаются. Это расширение runtime-схемы, не новый формат save и не миграция кампании.

### Команда и прибытие

`sendShip` принимает только `kind`, `factionId`, `expectedTurn`, `systemId` (источник), `shipId`, `destinationId`. После проверки схемы/номера хода/активной стороны идут:

1. Собственная исходная колония; иначе `NOT_OWN_COLONY`.
2. Свой размещённый ID именно в указанном источнике; иначе `SHIP_NOT_FOUND`. Чужой/отсутствующий/ещё готовый проект не отправляется.
3. Отсутствие transit; повтор/перенаправление — `SHIP_IN_TRANSIT`.
4. Собственная целевая колония; чужая/нейтральная — `NOT_OWN_COLONY` без раскрытия владельца, до проверки маршрута.
5. Прямой двусторонний переход в другую систему; одинаковая/несоседняя собственная цель — `INVALID_ROUTE`.

Успех добавляет только независимый transit. Отправка бесплатна, не меняет ход/казны/производство/галактику/счётчик ID. Новая колония и только что размещённый корабль могут сразу участвовать. MAX_ORDER_ID и MAX_TURN не блокируют отправку; на MAX_TURN завершение хода невозможно — явное техническое ограничение.

**Первый успешный собственный endTurn завершает все собственные перелёты**, в том числе отправленные в текущем окне действий. Чужой endTurn их не продвигает. Доход, FIFO-производство, прибытия и turn+1 применяются одной операцией: RESOURCE_LIMIT/TURN_LIMIT отменяет всё, а не только доход. `advanceShipTravel` копирует и проверяет список; endpoint ownership и очередность проверяет session API, helper не заменяет командную границу.

### Проекция и UI

SessionView содержит только свои глубоко отделённые ships/transit, чужие маршруты скрыты даже после разведки обоих концов. Это локальное разделение представлений, не multiplayer security. Для списка присутствующих кораблей используется `isShipAtColony`: transit исключён из обеих колоний до прибытия; общий счётчик остаётся по всем ships. После прибытия существующая страница показывает корабль в цели. Отправка имеет отдельное сообщение MainScene, но controls и список маршрутов добавятся отдельно.

### Проверки S3.9 и следующий шаг

[campaignTravel.test.ts](../tests/campaignTravel.test.ts) —63 чистые регрессии, [campaignScene.test.ts](../tests/campaignScene.test.ts) —1 новая (всего51, fake Phaser). Общий набор708/23 файла, strict typecheck/build прошли. Проверены направления/стороны, own/active/stale, malformed-схемы, сохранность/независимость/frozen/replay, hidden enemy, caps, атомарный endTurn и связка с производством/колонизацией. Программный smoke настоящего Phaser: обычный ход, диагностический completed→deploy→send→arrival, отсутствие на endpoints до прибытия, общий счётчик и reset; localStorage неизменен. Не mouse E2E отсутствующего UI, [отчёт](../reports/2026-09-09/07-campaign-travel.md).

## Интерфейс перелёта S3.10

В своей колонии откройте **Производство → Перелёты**. [TravelPanel](../src/ui/TravelPanel.ts) заменяет очередь отдельным экраном; «← Производство» возвращает её. Верхний список — стационарные корабли выбранной колонии, стрелки выбирают ID/имя. Стрелки цели показывают только соседние собственные колонии из projected systems/lanes. «Отправить» вызывает прежний sendShip, без оплаты и смены хода. Нет корабля/цели — кнопка отключена; неактивная сторона получает доменный отказ.

Нижний список — **все собственные** корабли в пути, независимо от выбранной колонии: страница/ID/имя, исходная система → цель,1 свой ход. Отправленный корабль исчезает из верхнего списка, новый маршрут выбирается в нижнем. После успешного своего endTurn маршрут исчезает, корабль появляется в цели; чужой ход/отказ endTurn не имитирует прибытие. Общий счётчик включает transit. Отдельной команды перенаправления нет.

TravelPanel получает только SessionView/callbacks; полный CampaignSession/определение галактики ей не передаются. Кнопка замыкает показанные shipId/destinationId, сцена — factionId/systemId/expectedTurn. Destroy дочерней панели блокирует прежние callbacks; новый клик новой кнопки может отправить следующий корабль. Смена стороны не раскрывает прежние маршруты. Изменённая после показа цель/корабль/номер хода отклоняются доменом, UI принимает state только при успехе.

Сцена владеет travelOpen/destinationIndex/transitPage, использует общий shipsPage и ограничивает страницы списками. Смена корабля сбрасывает цель. Переключение стороны/колонии/карты, reset/shutdown очищают режим/цель/страницу маршрутов; библиотека от навигации не перечитывается и не записывается. Pending блокирует все controls. ESC последовательно отменяет pending, закрывает перелёты в производство, производство на карту, затем запрашивает меню. Цепочка CampaignPanel→ProductionPanel→TravelPanel освобождается при redraw/shutdown.

Имена нормализуются только для текста и сокращаются по реальному text.width до745 px, цель до400 px; snapshot не меняется. Прямого перехода к цели на карте нет: закройте производство и выберите её обычной кнопкой карты. Это небольшой локальный UI, не флот/снабжение.

### Проверки S3.10 и следующий шаг

22 новых теста в [campaignScene.test.ts](../tests/campaignScene.test.ts), всего73 сценовых/730 общих в23 файлах, typecheck/build. Это fake Phaser, не hit testing. Настоящий браузер отдельно проверил мышью открытие/отправку/отказ неактивной стороне/выбор цели/endTurn, программный paid→deploy→travel цикл и обратный маршрут. Диагностические100 кораблей,80-символьные имена/большие ID, страницы/stale/pending/ESC/reset; localStorage неизменен. [Отчёт](../reports/2026-09-09/08-travel-ui.md).

## Приёмочная сверка S3.11

Принят существующий **мирный** цикл обеих сторон: разведка → своя соседняя колония → накопление дохода → оплаченный проект → готовность → размещение → отправка → прибытие. Это не приёмка всего S3: угрозы, технологии, стратегического боя, AI, победы и save ещё нет.

Два сквозных варианта в [campaignScene.test.ts](../tests/campaignScene.test.ts) используют готовый пресет либо проект из библиотеки, настоящие ProductionCatalog/ShipDesignManager/session API, fake Phaser и временный StoragePort. Стартовые средства, ownership, производство и ships не подставляются. Оба варианта проходят синий и красный цикл в одной партии, проверяют отсутствие записи библиотеки и сохранность оплаченного snapshot после refresh/постройки/перелёта.

Контрольные точки для истребителя185 кредитов/11 минералов/4 своих хода; ниже суммы текущих регрессий S3.20 с историческим сравнением, не подтверждённый баланс:

| Точка | Состояние |
| --- | --- |
| Новые колонии | Blue основывает Эдем, red — Узел; на стартовые100/50 покупка отклоняется |
| Ход11 | Обе казны200/100 после5 собственных начислений; расходы ещё не выполнены |
| Ход19 | Заказы ID1/2 закончены в Соле/Веге, обе казны95/129 |
| Отправка | ID/проект/казна/ход сохранены; до своего endTurn в целевой колонии корабля нет |
| Ход21 | ID1 синий в Эдеме, ID2 красный в Узле; transit/очередей/готовых нет, fuel2; обе казны114/139 в S3.20 (исторически115/139 без содержания) |

Историческая проверка S3.11: общий набор732 теста/23 файла,75 сценовых; typecheck/build прошли, JS прежний1646.29 kB/gzip388.38 kB. Исправления игровой логики не потребовались. Настоящий браузер повторил пресетный цикл без подстановки состояния с прежними115/139: мышью заказ/размещение/отправка/прибытие/отказ неактивной стороне, программно география/накопление/прогресс и часть навигации. LocalStorage не менялся, новая партия/выход/повторный вход чистые. Это не полный mouse E2E; библиотечный вариант браузером заново не проходился. [Отчёт](../reports/2026-09-09/09-strategic-cycle-acceptance.md).

S3.12 сохранил тогдашнюю экономику и ID/проекты, добавив fuel2 после одного перелёта. S3.20 адресно изменил итог на114/139 из-за оплаты одного размещённого корабля; прежние инварианты сохранены. Упоминания отсутствующего UI/топлива/содержания в ранних разделах описывают прежние этапы, не текущую модель.

## Стратегическое топливо S3.12

[campaignShips.ts](../src/domain/campaignShips.ts) хранит правила вне тактической энергии, массы, груза и ShipDesign v2. Это предварительный баланс одинаковый для боевых и гражданских корпусов, не реальная дальность/размер двигателя.

| Параметр | Правило |
| --- | --- |
| Ёмкость | CAMPAIGN_FUEL_CAPACITY = 3 стратегические единицы, не отдельное mutable-поле корабля |
| Начальный бак | deployProduction добавляет fuel3; начальный комплект включён в постройку, дополнительной оплаты нет |
| Текущий запас | Обязательный fuel, целое0..3, в том числе в пути. Нет default/coercion |
| Переход | TRAVEL_FUEL_COST = 1 при успешном sendShip; ноль после отправки допустим |
| Прибытие | Без дополнительного расхода/возврата/автозаправки, прежний own endTurn |
| Заправка | Только стоящий собственный корабль в указанной своей колонии, до полного бака |
| Цена | За недостающую единицу5 credits и2 minerals; пустой бак15/6, fuel1→10/4, fuel2→5/2 |
| Время | Заправка не меняет turn, не начисляет доход и не двигает очередь/корабли |

### Атомарность и команды

`sendShip` сохраняет прежний payload. После ownership/ID/transit/назначения/adjacency проверяется fuel≥1, иначе `INSUFFICIENT_FUEL`. Успех одновременно уменьшает fuel на1 и создаёт transit, казны неизменны. Неверная цель не расходует топливо. Отказ более позднего endTurn не откатывает уже успешный send: корабль остаётся в пути с оплаченным топливом. Ресурсы казны не заменяют пустой бак автоматически.

`refuelShip` принимает только `kind`, `factionId`, `expectedTurn`, `systemId`, `shipId`. Порядок: strict state/command → STALE_TURN → NOT_ACTIVE_FACTION → NOT_OWN_COLONY → SHIP_NOT_FOUND → SHIP_IN_TRANSIT → FUEL_FULL → INSUFFICIENT_RESOURCES. Полный бак — отказ без оплаты даже при нулевой казне. Недостаток любого ресурса отменяет всю операцию; частичного наполнения нет. Успех списывает оба ресурса и устанавливает fuel3 на независимом state. Чужие/готовые/отсутствующие ID не заправляются; refuel не перемещает корабль.

`getRefuelQuote(fuel)` проверяет число и возвращает независимые `{amount, cost:{credits,minerals}}`; неверный query бросает ZodError. Доступность/ownership/turn проверяет сессия, не quote. Клиент не передаёт amount/cost/fuel/capacity/design; лишние поля запрещены. Лимиты100 ships/MAX_ORDER_ID/MAX_TURN не запрещают заправку, но терминальный turn блокирует последующее прибытие. Повтор refuel после успеха получает FUEL_FULL; replay старого snapshot детерминирован, не network deduplication. `expectedTurn` не revision каждой команды внутри хода.

### Совместимость, проекция, интерфейс

Текущий `CampaignShip = {id, factionId, systemId, design, fuel, transit?}`. Производственные записи не содержат fuel. SessionView выдаёт запас только своих кораблей, вместе с независимыми проектами/transit; чужой запас скрыт даже после разведки. Локальная модель не multiplayer-авторитет и не доказывает историческую оплату.

**Старые runtime-записи кораблей без fuel отклоняются как INVALID_STATE**, не получают полный бак автоматически. Пустые новые партии остаются валидны. После HMR старой схемы начать новую партию; сохранений/миграции кампании нет. Библиотека ShipDesign v2, корабельные формулы/тактические состояния и формат completed не менялись.

В UI исправлены подписи расхода и границы «заправка пока только через API», добавлено корректное сообщение refuel. Запас/цена/кнопка заправки намеренно ещё отсутствуют; send пустого корабля показывает доменный отказ. В текущем пользовательском UI после трёх переходов нельзя заправиться — это следующий S3.13, не завершённый топливный интерфейс.

### Проверки S3.12 и следующий шаг

55 новых доменных проверок в [campaignFuel.test.ts](../tests/campaignFuel.test.ts),1 сценовая проверка пустого бака, всего788/24 файла,76 сценовых. Старые deployment/travel/acceptance фикстуры явно получили fuel; тест пересечения ID не содержит лишнее топливо в production, поэтому продолжает проверять именно пересечение. Strict typecheck/build прошли;62 модуля,1647.11 kB/gzip388.67 kB, прежнее предупреждение >500 kB.

Браузерный blue сценарий: через обычные события кнопок накоплены ресурсы, построен/размещён истребитель на turn19, полный бак3. Первая отправка мышью, следующие отправки/навигация/endTurn программными событиями:3→2→1→0, на turn25 отказ без изменения state в Эдеме. Прямой вызов команды сцены refuelShip (кнопки нет) списал15/6:155/159→140/153, fuel3; новая отправка дала2 без смены хода. Оплаченный проект/localStorage неизменны; reset turn1/0ships/1panel/1ESC/1canvas. State/казны/колонии/корабли не подставлялись. Не полный mouse E2E; red/refuel-границы проверены тестами, не заново браузером. [Отчёт](../reports/2026-09-09/10-campaign-fuel.md).

Интерфейсный шаг S3.13 выполнен ниже. Топливные коэффициенты S3.12 не изменены; группы добавлены S3.14–S3.17, содержание — S3.20. Снабжение/бой/AI/save отдельно, S3 открыт.

## Интерфейс заправки S3.13

В своей колонии откройте **Производство → Перелёты**, стрелками выберите стоящий корабль. Под именем — «Топливо: N/3», состояние бака, количество недостающего топлива и **полная** цена пополнения. «Заправить» немедленно исполняет refuelShip; подтверждения нет. При успехе обновляются бак/казна, страница и цель сохраняются, ход не меняется. Цена берётся из getRefuelQuote, не отдельной формулы UI.

Полный бак показывает3/3, нулевую цену и отключённую кнопку. Нет стоящего корабля — нет цены, кнопка отключена. Корабли в пути не заправляются. Отсутствие соседней собственной цели не мешает заправке в текущей колонии. При недостатке средств или наблюдении неактивной стороны кнопка остаётся доступной и показывает доменный отказ без частичной оплаты. Пустой send также продолжает показывать доменный отказ.

[TravelPanel](../src/ui/TravelPanel.ts) замыкает показанный shipId, [MainScene](../src/scenes/MainScene.ts) — показанные factionId/systemId/expectedTurn. Цена/fuel/amount не передаются. Только независимая own SessionView, без полного state или чтения библиотеки. Повтор старого callback после redraw/смены корабля/стороны/колонии/выхода инертен. Пока pending подтверждение, refuel и фоновые controls отключены. ESC последовательно отменяет подтверждение, закрывает travel, производство, затем запрашивает меню. Reset/shutdown освобождают виджеты/обработчики.

Запас/цена размещены отдельными строками, имя до745px, цена до570px; цель/отправка и нижние маршруты сдвинуты, не перекрывают заправку. Чужие корабли/запасы/цены не показываются. После прибытия топливо появляется в списке целевой колонии, автоматического пополнения нет.

19 новых регрессий в [campaignScene.test.ts](../tests/campaignScene.test.ts), всего807/24 файла,95 сценовых; strict/build прошли. Браузер: обычная партия обеих сторон до paid→deploy→send→arrival программными кнопками, mouse refill blue/red по5/2 на ходе21/22, mouse inactive отказ после красного возврата. Snapshot/localStorage сохранны. Диагностически red fuel0/казна14/6/имя80символов: отказ недостатка, ширина имени744px, pending/stale/ESC. Сброс и exit/reentry чистые. Это не полный mouse E2E; диагностические данные не выдаются за заработанные ресурсы. [Отчёт](../reports/2026-09-09/11-refuel-ui.md).

## Стационарные группы S3.14

[campaignFleets.ts](../src/domain/campaignFleets.ts) определяет чистую группу `CampaignFleet = {id, factionId, systemId, shipIds}`. Это список ссылок на стратегические корабли, не тактический флот и не копия ShipDesign/топлива/боевого состояния. Членство хранится только здесь: поля fleetId на ship нет.

| Правило | Ограничение |
| --- | --- |
| Состав | 2–10 уникальных собственных размещённых кораблей, стоящих в одной своей колонии |
| Членство | Каждый ship максимум в одной группе; transit не допускается |
| Количество | До20 групп на сторону,40 вместе; участники по-прежнему входят в лимит100 ships |
| Идентификаторы | Отдельная глобальная последовательность групп1..1e9, не production/ship ID; после расформирования не переиспользуются |
| Цена/время | Создание и расформирование бесплатны, не меняют ход, топливо, казны и производство |

`CampaignSession.fleets = {lastFleetId, items}` обязательно, новая партия содержит0 и пустой массив. Items строго возрастают по ID, выданные ID не больше счётчика; порядок shipIds сохраняет входной порядок. Сессия дополнительно проверяет существование каждого участника в ships, совпадение владельца/колонии и отсутствие transit. Чужой, готовый, несуществующий или стоящий в другой колонии корабль не может быть участником. Все схемы строгие, лишние поля отклоняются.

### Команды и совместимость

- `createFleet`: только kind/factionId/expectedTurn/systemId/shipIds. Общий порядок state→command→STALE_TURN→NOT_ACTIVE_FACTION→NOT_OWN_COLONY; затем каждый участник по входному порядку: SHIP_NOT_FOUND→SHIP_IN_TRANSIT→SHIP_IN_FLEET. После всех участников FLEET_LIMIT→FLEET_ID_LIMIT. Только после проверок выделяется ID и добавляется целая группа; частичной регистрации нет.
- `disbandFleet`: только kind/factionId/expectedTurn/systemId/fleetId. После общих проверок свой ID должен находиться именно в указанной колонии, иначе FLEET_NOT_FOUND. Удаляется только группа; все корабли остаются, lastFleetId сохраняется. Повтор получает отказ. MAX_TURN/MAX_ORDER_ID не мешают этим двум командам; исчерпание группового счётчика блокирует только создание.
- `sendShip` участника после own/source/transit проверок, но до проверки цели/маршрута/топлива, возвращает SHIP_IN_FLEET: сначала расформировать группу. Автоматического исключения участника или переноса группы нет.
- `refuelShip` участника работает по прежней цене и правилам, membership не меняет. Общего бака и групповой заправки нет. EndTurn не перемещает и не расформировывает группы; свободные корабли летают как раньше.

Успех — полностью независимый state, отказ — без state/мутаций входов. SessionView.fleets содержит только собственные независимые группы/массивы shipIds, без lastFleetId. Пропуски ID не гарантируют multiplayer-секретность; локальная схема не доказывает историческую оплату. `isShipInFleet` — селектор уже проверенного state/view, не самостоятельная командная граница.

**Старые runtime-сессии без fleets отклоняются как INVALID_STATE, даже с пустыми ships.** Нет default/автомиграции/save; после HMR начать новую партию. ShipDesign v2/библиотека и тактические экземпляры не менялись.

### Интерфейс и проверки

MainScene поддерживает сообщения двух команд, но кнопок создания/расформирования и списка групп пока нет. В старом интерфейсе все участники видны как корабли, доступны для заправки; send показывает доменный отказ. Это намеренно модельный этап, управление группами пока через API.

94 новых теста в [campaignFleets.test.ts](../tests/campaignFleets.test.ts): обе стороны/колонии, strict/cross-state, лимиты/ID/turn, повтор/отказы/атомарность, frozen/replay/независимость/приватность, обычные два оплаченных проекта FIFO→deploy→group→disband. Одна новая сценовая регрессия с fake Phaser проверяет send/refuel/reset при диагностическом membership. Всего902/25 файлов,96 сценовых; typecheck/build проходят,63 модуля,1650.82 kB/gzip389.81 kB, прежний chunk warning.

Настоящий Phaser программно: обычные разведка/колонизация/доход/оплата двух истребителей/размещение к turn45; createFleet API→отказ кнопки send без изменения state→disband API→перелёт туда/обратно→createFleet ID2→refuel участника на turn49 по10/4 (210/268→200/264). Проекты/localStorage неизменны, own проекция без чужих групп, reset/exit/reentry чистые. Кнопки вызваны событиями, групповые команды через API; без подстановки игрового state и ручного game.step. Не mouse E2E; красная группировка и пределы проверены тестами, не браузером. [Отчёт](../reports/2026-09-09/12-campaign-fleets.md).

## Интерфейс групп S3.15

В своей колонии откройте **Производство → Группы**. Группы, перелёты и очередь — взаимоисключающие вкладки, не наложенные панели. Верхний список содержит только свободные стоящие корабли этой колонии: стрелки показывают ID/имя/топливо, «Выбрать» добавляет ID в черновик выбора, «Убрать» исключает. Выбранность сохраняется между страницами; показан счётчик из10. «Очистить» убирает весь выбор, не расформировывает существующие группы.

«Создать» доступна при2–10 выбранных кораблях. При10 добавить ещё одного нельзя, исключить выбранного можно. Отказы при20 группах/пределе ID/неактивной стороне остаются видимыми доменными сообщениями, без частичного создания. Успех очищает выбор и открывает новую группу. Корабли уже в группе, в пути, чужие или в другой колонии не предлагаются как кандидаты.

Нижний список показывает группы выбранной колонии и общий счётчик собственных групп/20. Стрелки группы переключают ID/количество кораблей; отдельные стрелки участников показывают каждый ID/имя/топливо. «Расформировать» немедленно удаляет выбранную группу, оставляя все корабли на месте; дополнительного подтверждения нет. Это бесплатные операции без смены хода. У пустых списков управление отключено. Редактирования состава/группового перелёта нет.

Заправка участника — во вкладке «Перелёты». Там показан ID его группы и подсказка о расформировании перед одиночной отправкой. Кнопка отправки продолжает показывать доменный отказ для участника; автоматического исключения нет.

### Контракты и жизненный цикл

- [FleetPanel](../src/ui/FleetPanel.ts) получает только SessionView/выбранную колонию/UI-state/callbacks, не полную сессию/каталог/хранилище. [ProductionPanel](../src/ui/ProductionPanel.ts) создаёт и уничтожает дочернюю панель.
- Сцена хранит черновик ID и три страницы: кандидатов/групп/участников. Render удаляет из черновика недопустимые ID и ограничивает страницы текущими списками. Групповое членство не дублируется в UI-state.
- Create захватывает независимый массив показанных ID, disband — показанный ID группы; MainScene добавляет показанные factionId/systemId/expectedTurn. При изменении state до клика модель отклоняет старый payload, а не выбирает другие корабли. После перерисовки старые callbacks инертны через disposed. Принимается только ok; отказ сохраняет ещё допустимый выбор.
- Успешное создание очищает draft и выбирает новую группу; расформирование сбрасывает страницу участника/ограничивает группу; успешный endTurn очищает выбор. Смена стороны/колонии/карты/перелётов, закрытие групп, reset/shutdown очищают draft/страницы. Каталог при этом не перечитывается.
- Pending блокирует все групповые controls и вкладки; отмена сохраняет выбор. ESC: отмена pending → группы в очередь → очередь на карту → запрос меню. Reset/exit освобождают панели/обработчики/состояние, повторный вход чистый.
- Текст имени сокращается только для показа по реальной text.width (кандидат745px, участник590px); snapshot проекта неизменен. Заголовок производства ограничен290px, не перекрывает кнопку групп.

### Проверки S3.15 и следующий шаг

29 новых регрессий в [campaignScene.test.ts](../tests/campaignScene.test.ts), всего931/25 файлов,125 сценовых; typecheck/build прошли. Fake Phaser проверяет payload/отказы/2..10/лимиты, оба владельца/колонии/приватность, страницы, lifecycle и длинные имена, но не настоящее hit testing.

Настоящий Phaser: обычный paid→completed→deploy двух истребителей каждой стороны подготовлен событиями кнопок. Мышью проверены создание обеих групп, участник, расформирование синей группы, отказ send до него/успешный send после, отказ красного disband неактивной стороной. Проекты/localStorage неизменны. Диагностически100 красных ships/20groups/10selected/80-символьные имена/ID1e9: лимит/пагинация/pending/stale, ширина кандидата744px/участника577px, скриншоты осмотрены. Данные восстановлены; reset/exit/reentry чистые. Это не полный mouse E2E, [отчёт](../reports/2026-09-09/13-fleet-ui.md).

## Групповой перелёт S3.16

`sendFleet` отправляет всю группу в соседнюю собственную колонию. Каждый участник платит TRAVEL_FUEL_COST=1 из своего бака; если хотя бы одному не хватает, **вся отправка отклоняется без списания и частичного вылета**. Казна/номер хода/ID/проект/состав не меняются. Нет общего бака, дополнительной платы, зависимости от корпуса/массы/тактической скорости.

### Единственный маршрут и согласованность

Маршрут хранится только в уже существующем `CampaignShip.transit = {destinationId, remainingTurns:1}`. У группы новых persisted полей нет; group.systemId и member.systemId в пути означают исходную колонию, не присутствие в ней. Сессия строго требует:

- Участники существуют, имеют одного владельца и systemId группы, членство уникально как прежде.
- Либо все стоят, либо все имеют transit с одной целью/remainingTurns. Частичный вылет/прибытие и разные даже индивидуально допустимые маршруты — INVALID_STATE.
- Оба конца маршрута свои, переход прямой по общим lanes. Потеря ownership/участника делает state недопустимым; захват/потери ещё не реализованы.

`getFleetTransit(fleet, ships)` возвращает независимую копию маршрута первого участника, `isFleetAtColony` исключает путешествующую группу из обеих колоний. Это селекторы **проверенной сессии/её проекции**, не самостоятельный обход unknown-валидации. Не добавлять второй маршрут в группу: согласованность проверяется в session boundary.

### Команда, отказы и прибытие

Payload: только kind/factionId/expectedTurn/systemId/fleetId/destinationId. Порядок проверки: state → command → STALE_TURN → NOT_ACTIVE_FACTION → своя исходная колония → свой ID именно там (FLEET_NOT_FOUND) → отсутствие перелёта (FLEET_IN_TRANSIT) → своя цель → прямой переход → топливо всех участников (INSUFFICIENT_FUEL). Клиент не задаёт список участников, топливо, стоимость или длительность.

Успех создаёт отдельный transit каждого участника и уменьшает каждый fuel на1 на глубоко отделённом state. Остаток0 валиден, целиком пустая казна не мешает. Повтор send/перенаправление и disband в пути получают FLEET_IN_TRANSIT до изменения маршрута/членства. Single send/refuel/create с участником в пути получают прежний SHIP_IN_TRANSIT. Стоящего участника можно заправить, single send требует предварительного disband.

На первый успешный **собственный endTurn** все участники и группа прибывают вместе: group.systemId/ship.systemId меняются на цель, member.transit удаляются. Membership/порядок/ID/проект сохранны, нового расхода/возврата/автозаправки нет. Доход, FIFO, свободные корабли и группы продвигаются одной атомарной командой. Чужой ход не двигает группу. RESOURCE_LIMIT/TURN_LIMIT не допускают частичных прибытий и не возвращают топливо, ранее потраченное успешной отправкой.

В пути группа и участники продолжают занимать места в лимитах20 групп/100 ships стороны. Исчерпание MAX_ORDER_ID/MAX_FLEET_ID не мешает отправке; MAX_TURN допускает вылет, но блокирует последующее завершение хода — прежняя техническая граница прототипа. После прибытия отправка обратно/расформирование требуют новую исходную колонию и очередь владельца.

### Проекции, совместимость и текущий UI

SessionView по-прежнему содержит только own fleets+ships, все вложенные массивы/снимки/маршруты независимы. Чужие маршруты/топливо не раскрываются даже после разведки endpoints; локальные ID не multiplayer security.

Стационарные state S3.15 **совместимы без миграции/default**, новых обязательных полей нет. Старые state без fleets/ships/fuel по-прежнему отклоняются. Это runtime-контракт, не сохранение кампании; ShipDesign v2/библиотека/тактика не менялись.

FleetPanel показывает только группы, присутствующие в выбранной колонии через isFleetAtColony. В пути местных controls/участников группы нет ни в источнике, ни в цели; общий счётчик учитывает группу. После arrival она появляется целиком в цели. TravelPanel показывает участников как отдельные собственные корабли в пути. В S3.16 sendFleet был API-only; S3.17 подключает отдельную панель отправки/маршрутов ниже.

### Проверки S3.16 и следующий шаг

[campaignFleetTravel.test.ts](../tests/campaignFleetTravel.test.ts):66 новых тестов atomic fuel/arrival, обоих владельцев/направлений/нескольких групп/free ships, own/stale/active, schemas/partial/divergent routes, caps/terminal/frozen/replay/privacy и обычного paid цикла. В [campaignScene.test.ts](../tests/campaignScene.test.ts)3 новые регрессии на скрытие в endpoints/arrival, captured disband в пути и pages/reset с fake Phaser. Всего1000/26 файлов,128 сценовых, strict/build64модуля,1657.91 kB/gzip391.67 kB; прежний chunk warning.

Браузер программно: обе стороны оплачивают/размещают по два корабля, группы создаются существующими кнопками, sendFleet через API и endTurn переносят их целиком. Синяя группа проходит три перехода до0/0; после refuel одного отказ при3/0 не меняет state, после refuel второго send даёт2/2. Disband в пути отклонён, проект/localStorage неизменны, reset/exit/reentry чистые. Без подстановки игрового state, ручного game.step или mouse E2E; [отчёт](../reports/2026-09-09/14-fleet-travel.md).

## Интерфейс групповых маршрутов S3.17

В своей колонии откройте **Производство → Группы → Маршруты**. Отдельная [FleetTravelPanel](../src/ui/FleetTravelPanel.ts) заменяет экран выбора состава; «← Группы» возвращает к нему. Владение панелью/destroy у FleetPanel, сессией — у MainScene. Только собственная SessionView, без каталога/full state/localStorage.

Верх: группы, присутствующие именно здесь через isFleetAtColony, по одной странице, ID/число участников. Минимальный бак и число пустых вычисляются из ships, не сохраняются. Цели только explored own соседи по projected lanes. send замыкает показанные fleetId/destinationId; сцена добавляет показанные factionId/systemId/expectedTurn. Пустые списки/pending отключают кнопку; недостаток топлива и inactive остаются кликабельны для строгого доменного отказа. Никакого самостоятельного списания/переопределения стоимости/состава UI не делает.

Низ: все собственные группы в пути независимо от выбранной исходной колонии. Страницы показывают ID/состав/source→target/оставшийся свой ход, общий лимит20 групп/100 ships учитывает путешествующих. После успешного собственного endTurn маршрут исчезает и группа появляется целиком в целевой колонии с прежним остатком топлива. Участники по-прежнему отдельно видны в одиночных Перелётах. Controls расформирования/заправки/перенаправления в списке маршрутов отсутствуют.

Scene хранит только fleetTravelOpen/fleetDestinationIndex/fleetTransitPage и общий fleetPage. Выбор другой группы сбрасывает цель; успешный send выбирает новый маршрут, сбрасывает цель/страницу участника; render ограничивает страницы после удаления/прибытий. Toggle routes очищает черновик membership и маршрутные страницы, сохраняет выбранную группу. Смена стороны/колонии/карты/одиночных перелётов/reset/shutdown очищает весь UI-state. Отменённый pending сохраняет группу/цель; старые callbacks после redraw уничтожены. ESC: отмена pending → маршруты в группы → группы в очередь → очередь на карту → меню. Тексты нормализуются/сокращаются по реальной ширине, не меняя данные.

17 новых регрессий в [campaignScene.test.ts](../tests/campaignScene.test.ts),1017 тестов/26 файлов,145 сценовых: payload/страницы/arrival/fuel/refuel, обе стороны/privacy, captured stale/missing/moving/ownership, failed endTurn, empty/no-target/pending/ESC/reset/cleanup/caps. Fake Phaser не hit testing. В браузере оплаченные группы обеих сторон отправлены мышью, отказ при3/0 атомарный; полный цикл повторён событиями, snapshot/localStorage сохранны. Диагностически проверены20 групп/100 ships/10 участников/максимальный ID/2цели/20маршрутов. Физический ESC в интегрированном браузере не подтверждён; обработчик проверен событием и тестами. [Отчёт](../reports/2026-09-09/15-fleet-travel-ui.md).

## Приёмочная сверка S3.18

Принят существующий **мирный** цикл: обе стороны строят по два корабля из пресета либо сохранённого проекта, создают группы, совершают три перехода до пустых баков, заправляются, возвращаются четвёртым переходом домой и расформировывают группы. Правила S3.16/UI S3.17 не изменены. Это не закрывает S3: угрозы/исследования/бой/победа/save отсутствуют.

В [campaignScene.test.ts](../tests/campaignScene.test.ts) два новых it.each preset/library используют реальные ProductionCatalog, ShipDesignManager и session API; spies только наблюдают. Подменяются Phaser и временный StoragePort, но не казна/ships/галактика/turn. На каждую команду проверяются immutable input, payload/expectedTurn, собственная/чужая проекции, snapshot, точные расходы и отсутствие storage writes. Отдельно сверяется FIFO без переноса прогресса во второй заказ, arrival всего состава, инертность старых callbacks, reset/cancel/cleanup/reentry.

Таблица показывает текущие автоматизированные ожидания S3.20; прежние суммы без содержания оставлены для сравнения. До размещения расписание/экономика не изменены.

| Граница | Результат для каждой стороны |
| --- | --- |
| Ход29 после накопления | 380 кредитов/190 минералов, оплата двух fighter по185/11 |
| Ход45 после FIFO | 170/248; completed IDs в общем порядке1/3/2/4 |
| Ход47 после deploy/group и двух endTurn | Группы1=[1,2],2=[3,4], fuel3, казны188/258 (S3.18:190/258) |
| Ход53 после трёх перелётов каждой группы | Все баки0; отказ отправки атомарный |
| Перед четвёртым перелётом | Refuel одного за15/6 →3/0, отказ без изменений; второго за15/6 →3/3, send →2/2 |
| Ход55 после возврата | Каждая группа дома, fuel2, казны230/286 (S3.18:240/286) |
| Ход57 после disband/завершающих ходов | ships1/2 в Сол и3/4 в Веге, fuel2, групп0/lastFleetId2, казны248/296 (S3.18:260/296) |

В исторической браузерной приёмке S3.18 настоящий Phaser программно повторил оба варианта кнопками до прежних260/296 на turn57, без игровых инъекций/game.step. Для library реальный repository сначала сохраняет проект во временный StoragePort, каталог читает его вместо пользовательского хранилища только внутри синхронной подготовки; исходный global storage восстановлен в finally. Оплаченные snapshots и реальный localStorage сохранны. Reset/выход/reentry:0ships/0groups/turn1,1panel/1ESC/1canvas. Физический Escape через браузерную автоматизацию не отменил pending; обработчик проверен событиями, физический ввод остаётся ограничением проверки, не исправленным дефектом. Полный групповой цикл с новым содержанием повторён автотестами, не новым браузерным прогоном.

Всего1019 тестов/26 файлов,147 сценовых; strict/build65 модулей,1662.73 kB/gzip392.45 kB, прежний chunk warning. Это не mouse E2E, не баланс и не проверка полноценного сохранения кампании; single send после disband доступен, пятый перелёт не исполнялся. [Отчёт](../reports/2026-09-09/16-fleet-cycle-acceptance-tests.md).

## Проектирование содержания S3.19 — историческое решение

Выбран отдельный [контракт содержания](CAMPAIGN_UPKEEP.md):1 кредит за own размещённый ship на успешный own endTurn, без двойного учёта группы/transit; валовой доход проверяется на cap до списания, доступные средства расходуются частично, дефицит не блокирует ход и не создаёт долг/потери. Описаны quote, квитанция фактического платежа, собственный прогноз, совместимость и критерии реализации. Это простая предварительная модель с явно отмеченными ограничениями, не проверенный баланс.

На этапе S3.19 формулы/UI/суммы S3.18 ещё не менялись:1019 тестов/типизация/сборка подтверждали прежнюю базу, критерии исполнения оставались открыты. [Исторический отчёт S3.19](../reports/2026-09-09/17-upkeep-contract.md). Реализация выполнена следующим шагом ниже, полный бюджетный UI остаётся S3.21.

## Содержание кораблей S3.20 — текущий контракт

- [campaignUpkeep](../src/domain/campaignUpkeep.ts): `UPKEEP_CREDITS_PER_SHIP = 1`; `getUpkeepQuote(unknown ships, unknown faction)` строго проверяет существующие схемы, invalid query бросает ZodError. Считает все own `session.ships`, независимо от группы/transit/fuel/проекта, но не orders/completed. Размещённый до endTurn корабль платит сразу; членство не дублирует начисление.
- `calculateEndTurnEconomy` принимает уже проверенные типизированные Treasury/income/quote. Общий session-helper `economyFor` сначала проверяет MAX_TURN, затем валовую сумму казны и дохода по treasurySchema. RESOURCE_LIMIT до списания: даже если расход уменьшил бы итог до cap, команда отклоняется. Лишь после проверки списывается min(доступные кредиты, начисление); минералы не расходуются.
- Недостаток даёт `shortfallCredits`, не долг/отказ/потерю. На успешном endTurn FIFO, свободные корабли и группы прибывают, turn увеличивается атомарно с доходом/списанием. Схема state → command → stale → active сохранена; технический отказ не публикует state/квитанцию и не возвращает уже потраченное send-топливо.
- Только успешный endTurn возвращает `endTurnEconomy`: factionId, **завершённый** turn, валовой income, upkeep{shipCount,dueCredits,paidCredits,shortfallCredits}, treasuryAfter. Поле optional в успешном `SessionResult`, отсутствует у остальных команд; вложенные значения независимы от результата state и входа.
- Обязательный own `view.economyForecast` содержит `{ok:true,income,upkeep,treasuryAfter}` либо `{ok:false,code:'TURN_LIMIT'|'RESOURCE_LIMIT'}`. Даже неактивная сторона получает гипотетический расчёт без права завершить ход; TURN_LIMIT имеет приоритет. `view.income` остаётся валовым. Новый forecast после платежа — следующий расчёт, не прошлый платёж; чужая экономика/ships/дефицит не раскрываются.
- [MainScene](../src/scenes/MainScene.ts) показывает **фактическую квитанцию**, не будущий прогноз. Новых persisted полей, схем, версии ShipDesign, миграции или save нет; валидные S3.18 state совместимы, следующий endTurn уже платит, без ретродолга. Старые state без ships/fleets/fuel остаются недопустимыми.

### Проверки S3.20 и следующий шаг

72 новых теста [campaignUpkeep.test.ts](../tests/campaignUpkeep.test.ts) и3 сценовых:1094/27 файлов,150 сценовых, все прошли. Typecheck/build прошли:66 модулей,1663.51 kB/gzip392.77 kB, прежнее предупреждение >500 kB. Адресно обновлены числа прежних циклов, сохранены все immutability/enemy privacy/FIFO/fuel assertions; независимый endTurn oracle проверяет и квитанции. Групповые preset/library циклы обеих сторон подтверждают188/258 на turn47,230/286 на55,248/296 на57. Одиночный цикл —114/139 на21 каждой стороне.

Настоящий Phaser в видимой вкладке: свежая партия → колонизация → оплаченный одиночный пресет → FIFO → deploy → send → endTurn обеих сторон **событиями реальных кнопок, без игровых инъекций**. На turn21 казны114/139 каждой, ships в Эдеме/Узле с fuel2, квитанции paid1/due1; storage неизменен.

Отдельная **диагностическая подстановка**100 blue +1 red ship, синей казны5/5, дохода20/10 и группы из2 в transit дала после endTurn turn22, казну0/15, paid25/due100/shortfall75. Все101 корабль сохранены, группа прибыла в Сол с fuel0. Текст квитанции перенесён в304×67px и помещается в панель по screenshot. Диагностическое состояние восстановлено, временные данные удалены, затем reset до turn1/0ships/0fleets и выход.

Не полный mouse E2E и не повторная полная групповая браузерная приёмка; полный групповой цикл проверен автоматически. Ограничение физического ESC прежнее. [Отчёт S3.20](../reports/2026-09-09/18-campaign-upkeep.md).

На завершении S3.20 полноценный бюджетный UI ещё отсутствовал. Этот исторический предел снят следующим шагом; тариф не признан сбалансированным.

## Бюджетный интерфейс S3.21

[BudgetPanel](../src/ui/BudgetPanel.ts) получает только `CampaignSessionView`, без полного state, каталога или доменных расчётов. Показывает собственные текущую казну и валовой доход; только при успешном `economyForecast` — shipCount/dueCredits/paidCredits/shortfallCredits/treasuryAfter. При ошибке чисел результата нет: `RESOURCE_LIMIT` объясняет валовой cap до списания, `TURN_LIMIT` — предел хода. Текущая казна/доход не маскируются под успешный итог. Подпись различает прогноз текущего активного хода и условный прогноз неактивной стороны; это не авторизация команды.

Правило отображено явно: «Участие в группах и перелёты не дают скидок. Дефицит — не долг и не блокирует ход». Отдельная заметка направляет к фактическому платежу в сообщении справа: после `endTurn` там квитанция завершённого хода, а слева уже новый forecast.

- [CampaignPanel](../src/ui/CampaignPanel.ts) владеет и уничтожает дочернюю панель. `campaign-budget` расположен в x440,y114; слева взаимоисключающие карта/производство/бюджет. Бюджет не читает каталог.
- [MainScene](../src/scenes/MainScene.ts) добавляет только `budgetOpen`, не копию расчёта/квитанции. Toggle закрывает производство и вызывает `resetTravel`, включая черновик групп, но сохраняет сообщение фактического платежа. Открытие производства и callback выбора системы закрывают бюджет.
- Смена стороны сохраняет открытый бюджет, перестраивает own проекцию и очищает сообщение. Успешный `endTurn` обновляет бюджет, не закрывая его. Pending блокирует кнопку, старые callbacks после redraw инертны. ESC: сначала отмена pending, затем бюджет → карта → запрос меню; reset/shutdown закрывают бюджет.
- Домен, тариф/баланс, схемы, ShipDesign и библиотека не менялись.

### Проверки S3.21 и следующая приёмка

36 новых случаев в [campaignScene.test.ts](../tests/campaignScene.test.ts):186 сценовых,1130 тестов/27 файлов; typecheck/build прошли,67 модулей,1665.95 kB/gzip393.48 kB, прежний chunk warning. Первоначальные24 падения относились только к регистрозависимой проверке текста «груп» против «Группы»; подпись уточнена до приведённой выше, семантического исправления логики не было.

В новой видимой браузерной вкладке бюджет открыт мышью со свежей партии. Обычный оплаченный одиночный пресетный цикл обеих сторон исполнен **событиями кнопок, без игровых инъекций**: перед завершением прогноз114/139, затем квитанция paid1/due1 и казна114/139, новый условный прогноз133/149. Итог turn21 и114/139 обеих сторон, storage неизменен.

Отдельная **диагностика**, не обычный игровой путь:100 blue+1 red ship, синяя казна5/5 и доход20/10 → квитанция paid25/due100/shortfall75; рядом новый прогноз paid20/shortfall80. Оба текста помещаются по screenshot. Точное достижение gross cap допустимо, строка остатка531px; переполнение кредита показывает `RESOURCE_LIMIT`679×42px без успешных полей, отказ endTurn сохраняет state. При MAX_TURN приоритетная ошибка642px; команда неактивной стороны даёт `NOT_ACTIVE_FACTION`, активной красной — `TURN_LIMIT`.

Pending отключает кнопку, прежний callback инертен. Программное событие Phaser ESC отменяет pending, затем закрывает бюджет на карту — **не проверка физической клавиатуры**. Диагностическое состояние восстановлено, временные данные удалены, storage неизменен; reset дал turn1/0ships/`budgetOpen=false`, затем бюджет открыт и выполнен выход. Повторный вход требует отдельного финального подтверждения. Нет полного mouse E2E или нового полного группового браузерного цикла; прежние preset/library регрессии остаются248/296 на turn57.

## Приёмка экономики и бюджета S3.22

Добавлены6 диагностических сценовых случаев и расширены два существующих preset/library групповых сценария в [campaignScene.test.ts](../tests/campaignScene.test.ts), без изменения исходников игры/механик. Полный набор: **1136 тестов/27 файлов,192 сценовых**, strict typecheck/build прошли;67 модулей, JS1665.95 kB/gzip393.48 kB, прежний warning >500 kB.

Независимая браузерная проверка повторила **оба источника для обеих сторон** через события `pointerdown` настоящих кнопок, без подстановки игрового state. Накопление380/190 на turn29 → по два оплаченных истребителя → FIFO/deploy/группы → три перехода до0/0 и атомарный отказ → refuel одного/отказ3/0 → refuel второго → четвёртый переход домой → disband. На начале turn57 (после завершения56) у каждой стороны248 кредитов/296 минералов; четыре корабля дома с fuel2, групп0, `lastFleetId=2`.

Для каждого источника бюджет сверялся168 раз: по3 проверки на56 завершений хода. Прогноз до команды согласован с фактической квитанцией, новый прогноз на обновлённом state и own/inactive-контекст корректны. Оплаченные снимки и пользовательское хранилище неизменны. Library использует настоящий `ShipDesignManager.save/load` во временный `StoragePort`; подмена global storage ограничена синхронной подготовкой каталога и снимается в `finally`, пользовательская библиотека не перезаписывается.

**Диагностика отдельно от заработанного цикла:** браузерный preload21 own ships, двух колоний, казны0/5, группы из2 и свободного корабля в transit, FIFO head1/следующий4. Для обеих сторон paid20/due21/shortfall1, прибытия и FIFO продолжаются; gross credit/mineral cap+1 атомарно отклоняет ход. Диагностический state восстановлен. Тесты отдельно покрывают100 own против3 enemy: paid25/due100/shortfall75, следующий forecast paid20/shortfall80, без долга; выход из cap реальной командой refuel и ошибку будущего прогноза даже после успешной квитанции.

Родитель «Производство и содержание флотов, несколько ресурсов и очередь строительства» принят только в реализованных границах; [матрица с регрессиями](CAMPAIGN_UPKEEP.md#приёмка-s322-и-матрица-доказательств), [отчёт S3.22](../reports/2026-09-09/20-economy-budget-acceptance.md). Это не сбалансированное банкротство, военная кампания или снабжение. Браузерные события не являются mouse E2E, проверкой физического ESC или производительности. **S3 целиком открыт.**

## Проектирование S3.23 — историческая постановка после S3.22

После S3.22 требовалось определить контракт версионированного сохранения/загрузки кампании **отдельно от библиотеки `ShipDesignManager`**: версии payload и правил, полная проверка `CampaignSession` и межобъектных инвариантов, атомарный отказ загрузки без замены текущей партии, лимиты ресурсов/хода/ID/списков, transit и согласованность fleets. Разделить сохраняемую сессию и производные бюджетные данные; квитанцию не сохранять, прогноз пересчитывать. Эта постановка закрыта [контрактом S3.23](CAMPAIGN_SAVE.md); чистый кодек реализован S3.24, слот — S3.25, ручной UI — S3.26. Исторические результаты S3.22 выше не являются проверкой save API или AI S3.28.
