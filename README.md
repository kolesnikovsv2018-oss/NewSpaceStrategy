# Orion Space Strategy

Браузерный прототип 2D-стратегии в духе Master of Orion: конструктор компонентов и кораблей, демонстрация полёта и автоматический бой флотов.

## Стек и запуск

TypeScript strict, Phaser 3, Vite 4, Vitest 3. Используется Yarn Classic 1.22.22 и единственный yarn.lock. Рекомендуется Node 22+; проверено на Node 24.20.0.

| Команда | Назначение |
| --- | --- |
| `yarn install --frozen-lockfile` | Установка зависимостей |
| `yarn dev` | Dev-сервер, порт 3000 |
| `yarn test` | Регрессионные тесты в Node |
| `yarn test:watch` | Тесты в режиме наблюдения |
| `yarn typecheck` | Проверка типов исходников и тестов |
| `yarn build` | TypeScript и production-сборка в dist |
| `yarn preview` | Просмотр production-сборки |

## Структура

- src/scenes — сцены меню, верфи, демонстрации кораблей и боя.
- src/entities — модели, фабрики и менеджер боя; visuals — представления Phaser.
- src/ui — панели редактора и информации о корабле.
- src/domain — схема проекта, корпуса, чистые формулы и валидация.
- src/utils — проверяемое версионированное хранение проектов и миграция.
- tests — модельные регрессии и контрактные тесты визуализации с заглушкой Phaser.
- .github/skills — контекст и процедуры разработки.
- reports — планы и отчёты по датам.

## Текущее состояние

**S3.25 — однослотовый [CampaignSaveManager](src/utils/CampaignSaveManager.ts) реализован поверх [JSON-кодека S3.24](src/domain/campaignSave.ts).** Ключ `orion_campaign_v1`, необязательный внедряемый `StoragePort`, ленивый доступ к хранилищу; конструктор ничего не читает/не пишет. `load()` делает один `getItem` и передаёт текст настоящему decode; `save(unknown)` выполняет encode до любого доступа к storage, затем один атомарный `setItem`. Отказы типизированы, без сырых исключений; нет удаления, rollback, fallback, кеша или чтения перед записью. Подтверждение перезаписи — ответственность будущего UI. **Слот доступен через API, но интеграции с игровым UI ещё нет**: игрок пока не может сохранить партию кнопкой.

Полный прогон S3.25: **1505 тестов/29 файлов =1435 прежних+70 новых**,192 сценовых без изменений; strict typecheck и production build прошли. Сборка67 модулей, JS1665.95 kB/gzip393.48 kB, основной chunk `main-2586e329` прежний: менеджер не импортирован игровым entry. Warning >500 kB сохранён. Реальный кодек, UTF-8 exact5MB/+1, ошибки getter/методов, независимость и два заработанных preset/library цикла: checkpoint слота на turn31 → FIFO45 → deploy/group/send → checkpoint перед каждым своим endTurn →47,188/258 каждой стороне и fuel2. [Матрица S3.25](docs/CAMPAIGN_SAVE.md#12-фактические-проверки-s325), [отчёт S3.25](reports/2026-09-09/23-campaign-save-repository.md).

**Новый браузерный smoke S3.25:** в существующую страницу импортированы менеджер/сессия, явно передан временный Map-порт. Конструктор0 операций → отсутствующий слот → реальные explore/colonize/endTurn обеих сторон до3 → save/new manager/load/равенство следующего endTurn. Инъекция `DOMException` с `QuotaExceededError` дала одну неуспешную запись без изменения старого слота; invalid save не добавил записей. Всего2 чтения/2 попытки записи (успех+отказ); снимок пользовательского localStorage и сцена неизменны. Это **не реальная браузерная квота/диск/reload/UI/E2E или групповой браузерный цикл**.

Исторический S3.24 отдельно проверял только чистый кодек в свежей странице: round-trip/следующий endTurn на turn3, точные5MB/+1 и приоритет версии правил;1435 тестов/28 файлов. Эти проверки не выдаются за новый smoke слота. [Результаты S3.24](docs/CAMPAIGN_SAVE.md#11-фактические-проверки-s324).

**S3.22 — приёмка экономики и бюджета завершена:** оба источника проектов (пресет/библиотека) и обе стороны прошли оплаченный групповой мирный цикл с содержанием. Родительский пункт производства/содержания закрыт **только** в реализованных границах: два ресурса, предоплаченная FIFO-очередь, отмена с возвратом, размещение, покорабельное содержание и бюджет. [Матрица доказательств и ограничения](docs/CAMPAIGN_UPKEEP.md#приёмка-s322-и-матрица-доказательств), [отчёт S3.22](reports/2026-09-09/20-economy-budget-acceptance.md).

На этапе S3.22 добавлены6 диагностических сценовых случаев и расширены два прежних preset/library групповых цикла: **1136 тестов/27 файлов,192 сценовых**; strict typecheck и production build прошли,67 модулей, JS1665.95 kB/gzip393.48 kB, прежний warning >500 kB. Независимый браузерный прогон обоих источников для обеих сторон выполнен событиями `pointerdown` настоящих кнопок без подстановки игрового state: накопление380/190 на turn29 → по два оплаченных истребителя/FIFO/deploy/группы → четыре перехода с отказами0/0 и3/0 и двумя заправками → возврат/disband. Итог turn57: каждой стороне248/296, четыре корабля дома с fuel2, групп0, `lastFleetId=2`. На источник выполнено168 сверок бюджета (по3 на56 endTurn); прогнозы согласованы с квитанциями, новые own/inactive прогнозы корректны, снимки проектов и пользовательское хранилище неизменны. Дефицит/cap проверены отдельно диагностически. Это не mouse E2E, физический ESC или замер производительности.

Кнопка **«Бюджет»** показывает собственный `economyForecast`, не пересчитывая экономику в UI; условный прогноз неактивной стороны не разрешает командовать. Фактическая квитанция справа отделена от нового прогноза. Правила [содержания S3.20](docs/CAMPAIGN_UPKEEP.md) сохранены:1 кредит за свой размещённый корабль на свой успешный `endTurn`, валовой cap **до списания**, частичная оплата без долга/потерь/остановки FIFO или прибытия. В S3.22 новых механик и изменений исходников игры нет. Это не сбалансированная экономика банкротства, военная кампания или снабжение.

**Далее только S3.26 — ручной интерфейс save/load**, подтверждения перезаписи/замены партии, защита same-turn callbacks/lifecycle и браузерная save→reload→load-приёмка. S3.25 не добавляет автоопераций; атомарность одной записи обязательна для порта, между менеджерами действует last successful writer wins, не CAS. **S3 целиком и родитель сохранения открыты**, тариф содержания предварительный.

### История предыдущих шагов

Числа и ограничения ниже относятся к моменту соответствующего шага. S3.19 закрыл только [проектирование правил](reports/2026-09-09/17-upkeep-contract.md); исполнение добавлено [S3.20](reports/2026-09-09/18-campaign-upkeep.md), полноценный бюджетный UI — S3.21. Прежние утверждения об отсутствии UI исторические.

**[S3.21 — бюджетный UI](reports/2026-09-09/19-campaign-budget-ui.md):**36 новых сценовых проверок, на этом этапе1130 тестов/27 файлов,186 сценовых; strict/build прошли. Браузер подтвердил одиночный пресетный цикл обеих сторон и отдельные диагностические дефицит/пределы, но ещё не повтор всего группового цикла. Этот предел проверки снят S3.22 выше; прежние числа не являются текущим итогом тестов.

**[S3.18 — приёмка мирного цикла с группами](reports/2026-09-09/16-fleet-cycle-acceptance-tests.md):** обе стороны прошли постройку по пресету и сохранённому проекту → группу → расход топлива → заправку → возврат домой → расформирование. На этом этапе1019 тестов, типизация/сборка и программная проверка обоих вариантов в браузере прошли. Проекты и пользовательское хранилище не изменены. В текущих регрессиях S3.20 казны каждой стороны на turn47/55/57 равны188/258,230/286,248/296; прежние190/258,240/286,260/296 относятся к S3.18 без содержания.

**[S3.17 — интерфейс групповых маршрутов](reports/2026-09-09/15-fleet-travel-ui.md):** откройте **«Производство» → «Группы» → «Маршруты»**, выберите группу и соседнюю свою колонию, нажмите «Отправить». Показаны минимум топлива участников, число пустых баков и список собственных групп в пути. «← Группы» возвращает к составу; для заправки каждого корабля используйте «Перелёты». Правила и UI не менялись в S3.18. Физический ESC в интегрированном браузере требует ручной перепроверки.

**[S3.16 — модель группового перелёта](reports/2026-09-09/14-fleet-travel.md):** sendFleet отправляет всю группу в соседнюю свою колонию, тратя1 топливо каждого участника. При нехватке у любого корабля вся команда отклоняется. Прибытие целиком на свой endTurn; расформирование/заправка участников в пути запрещены. Старые стационарные группы совместимы. Правила не менялись в S3.17.

**[S3.15 — интерфейс групп](reports/2026-09-09/13-fleet-ui.md):** в своей колонии откройте **«Производство» → «Группы»**, выберите2–10 свободных кораблей стрелками и кнопкой «Выбрать», нажмите «Создать». Ниже — страницы собственных стационарных групп и их участников, топливо и «Расформировать». Выбор можно убрать/очистить; создание и расформирование бесплатны, без смены хода. ESC возвращает в очередь, затем на карту. Летящие группы не показываются в колонии до прибытия, но входят в общий лимит; их участники видны в «Перелётах».

**[S3.14 — модель групп](reports/2026-09-09/12-campaign-fleets.md):** максимум20 групп на сторону, корабль входит только в одну группу. Его можно заправить во вкладке «Перелёты», но перед одиночной отправкой нужно расформировать группу. Строгие команды и независимые собственные проекции, отдельные ID групп; старые runtime-сессии без fleets отклоняются, после HMR нужна новая партия. Правила модели и баланс топлива не менялись в S3.15.

**[S3.13 — интерфейс заправки](reports/2026-09-09/11-refuel-ui.md):** в «Производство → Перелёты» показаны топливо выбранного корабля, ёмкость и цена полного пополнения. «Заправить» списывает цену и наполняет бак без смены хода. Полный бак/нет корабля отключают кнопку; нехватка ресурсов и неактивная сторона дают отказ. Правила топлива/цены не менялись в S3.14.

S1 и S2 закрыты по [плану](reports/2026-09-07/plan.md): верфь → сохранение → загрузка → полёт/бой → корректировка. [Приёмка S2](reports/2026-09-08/11-s2-acceptance.md) фиксирует канонический путь проекта и явную границу legacy-совместимости, не полное удаление старого API. Общее тактическое ShipState отделено от проекта; фабричные пресеты, груз и служебные определения используют общую модель. Менеджер боя использует ICombatant, представления — readonly ShipView, полёт — FlightShip. Общие операции находятся в ShipRuntime. Регрессии включают стратегические производство/экономику/размещение/перелёты, топливо и группы. [Модель проекта и ограничения](docs/SHIP_DESIGN.md).

В верфи выберите или создайте модуль, настройте его параметры и установите в слот. «Сохранить» записывает проект в localStorage, «Загрузить» выбирает сохранённый, «Экспорт» выгружает сохранённую библиотеку JSON. Каталог хранится отдельно: его изменение не меняет уже установленные копии. «Полёт» и «Бой» испытывают текущий черновик без автоматического сохранения; возврат не переносит повреждения в проект.

Индикатор «Не сохранён» показывает новый или изменённый проект. Перед «Новый», «Стартовый», «Копия», выбранным пресетом/загрузкой и выходом в меню появляется подтверждение: отмена/ESC возвращает к черновику, продолжение не сохраняет его автоматически. После испытания несохранённый проект остаётся несохранённым. Защита действует внутри игры, не при закрытии или перезагрузке вкладки.

«Готовые проекты» в верфи загружает истребитель, фрегат, крейсер, дредноут, разведчик, грузовоз или добытчик для изменения и сохранения. Восемь посадочных мест включают два служебных. Трюм+ увеличивает объём; добыча/ремонт/сканирование пока только настраиваемые номиналы с явной пометкой о неисполняемых действиях. Обычный бой использует те же боевые проекты, стороны различаются цветом. В полёте можно загрузить/выгрузить тестовую руду: масса и объём ограничиваются независимо, груз снижает скорость и не сохраняется в чертеже.

Сохранения v2 используют отдельный ключ orion_shipyard_v2. Старая библиотека v1 автоматически читается с добавлением пустых служебных слотов, исходный ключ не изменяется. Сохранение/экспорт создают v2; старый клиент v1 не поддерживает новые модули.

DesignedShip больше не наследует legacy Ship/CombatShip и создаётся из состояния/характеристик проекта без искусственных компонентов старого формата. Общая тактика подключается к обоим путям независимо; движение, энергия и груз используют ShipRuntime. Старые фабрики изолированы в src/legacy; createCustomShip и прежний импорт ShipComponentFactory сохранены. Проектные powerSource/engine/cargoHold — выходные проекции с неизменяемой конфигурацией; запись текущей энергии сохранена с проверкой диапазона. Новые гражданские экземпляры создаются через ShipFactory.createFromDesign.

Единицы тактики явно подписаны: энергия ЭЕ, мощность ЭЕ/с, расстояния такт. ед., время симуляции в секундах. Неверные численные аргументы движения/энергии/урона не исполняются; новая оценка полёта учитывает генерацию и груз, но не боевые расходы или дискретные шаги. Это не световые годы и не топливо. Исполнение добычи/ремонта/сканирования не реализовано; тестовый груз не является экономикой. Изменение модулей предсказуемо меняет характеристики, но случайный бой пока невоспроизводим.

S3 реализуется поэтапно: [S3.1](reports/2026-09-08/12-campaign-domain.md) — чистая модель, [S3.2](reports/2026-09-08/13-campaign-map.md) — карта через кнопку «Галактика». Выберите название системы, выполните разведку/колонизацию; справа видны сведения и причины отказа. Новая партия/выход требуют подтверждения; ESC отменяет его. [Правила и ограничения](docs/CAMPAIGN.md).

[S3.3](reports/2026-09-09/01-campaign-economy.md) добавляет чистую экономику, [S3.4](reports/2026-09-09/02-campaign-economy-ui.md) подключает её к карте. Показаны кредиты/минералы и ожидаемый **валовой** доход наблюдаемой стороны, номер хода и активная сторона. «Завершить ход» начисляет доход своих колоний, с S3.20 списывает содержание и передаёт ход; наблюдаемая сторона остаётся прежней. «Сменить сторону · локально» меняет только наблюдение: для действий следующей стороны переключитесь на неё, очередь от этого не меняется. Команды неактивной стороны отклоняются. Партия не сохраняется, AI нет.

[S3.5](reports/2026-09-09/03-production-domain.md) — чистая очередь производства, [S3.6](reports/2026-09-09/04-production-ui.md) — её интерфейс. В своей колонии нажмите «Производство», выберите пресет или сохранённый проект: показаны цена/срок, очередь до трёх заказов, прогресс и отмена с суммой возврата. Библиотека только читается; неполный проект заказать нельзя. Готовые записи листаются стрелками. Браузерная проверка этого UI — в отчёте S3.6.

[S3.7](reports/2026-09-09/05-strategic-deployment.md) добавляет модель размещения, [S3.8](reports/2026-09-09/06-deployment-ui.md) — кнопку «Разместить» у выбранного готового проекта. Размещение сохраняет ID/проект, бесплатно и не меняет ход; лимит100 кораблей на сторону. «Корабли: N» показывает свои экземпляры в выбранной колонии постранично, «Готово: N» возвращает список готовых.

[S3.9](reports/2026-09-09/07-campaign-travel.md) — чистая модель перелёта одного корабля между соседними собственными колониями. Отправка не меняет казну/ход; с S3.12 расходует1 топливо, с S3.14 требует отсутствие группы. Прибытие — на первый успешный свой endTurn; повторная отправка в пути запрещена. Корабль в пути не показывается в обеих колониях до прибытия, но учитывается в общем лимите и, с S3.20, в содержании. Бой кампании, AI и законченная партия ещё не реализованы.

[S3.10](reports/2026-09-09/08-travel-ui.md) подключает интерфейс перелёта: в своей колонии откройте **«Производство» → «Перелёты»**, выберите корабль и соседнюю свою цель, нажмите «Отправить». Ниже показаны все собственные корабли в пути с маршрутом и сроком; после своего endTurn они появляются в цели. ESC возвращает сначала в производство, затем на карту. Бой/AI и UI сохранения кампании ещё не реализованы; дефицит содержания S3.20 не блокирует прибытие.

**[S3.11 — приёмочная сверка](reports/2026-09-09/09-strategic-cycle-acceptance.md):** цикл от разведки/колонизации до оплаченной постройки и прибытия принят для обеих сторон. Два сквозных варианта (пресет/библиотека) сохранены в текущих тестах; новый расход топлива не меняет оплаченный проект или ID.

**[S3.12 — стратегическое топливо](reports/2026-09-09/10-campaign-fuel.md):** новый корабль получает бак3 единицы, каждый переход расходует1 при отправке. Автозаправки при прибытии нет. refuelShip полностью пополняет бак стоящего своего корабля в своей колонии по5 кредитов/2 минерала за единицу, атомарно; S3.13 подключает кнопку. Старые runtime-корабли без fuel отклоняются; после HMR начать новую партию. Библиотека ShipDesign v2 и тактическая энергия не менялись. S3 целиком открыт.

## Разработка

Перед задачей читать [общие инструкции](.github/copilot-instructions.md) и [контекст проекта](.github/skills/orion-project/SKILL.md). После задачи обновлять навыки и создавать новый отчёт в каталоге текущей даты.

Контрактные тесты с заглушкой Phaser не заменяют браузерный smoke test. Тяжёлый JS chunk Phaser остаётся известным предупреждением сборки, не подавляется настройками.

## Лицензия

Проект распространяется под лицензией MIT.
